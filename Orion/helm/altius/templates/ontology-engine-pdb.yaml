{{- if gt (int .Values.ontologyEngine.replicaCount) 1 }}
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ include "altius.fullname" . }}-ontology-engine
  labels:
    {{- include "altius.labels" . | nindent 4 }}
    app.kubernetes.io/component: ontology-engine
spec:
  minAvailable: {{ .Values.ontologyEngine.pdb.minAvailable | default 1 }}
  selector:
    matchLabels:
      {{- include "altius.selectorLabels" (dict "name" "ontology-engine" "release" .Release.Name) | nindent 6 }}
{{- end }}
