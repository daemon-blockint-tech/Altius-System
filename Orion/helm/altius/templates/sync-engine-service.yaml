apiVersion: v1
kind: Service
metadata:
  name: {{ include "altius.fullname" . }}-sync-engine
  labels:
    {{- include "altius.labels" . | nindent 4 }}
    app.kubernetes.io/component: sync-engine
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.syncEngine.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "altius.selectorLabels" (dict "name" "sync-engine" "release" .Release.Name) | nindent 4 }}
