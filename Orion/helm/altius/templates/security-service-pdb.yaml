{{- if gt (int .Values.securityService.replicaCount) 1 }}
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ include "altius.fullname" . }}-security-service
  labels:
    {{- include "altius.labels" . | nindent 4 }}
    app.kubernetes.io/component: security-service
spec:
  minAvailable: {{ .Values.securityService.pdb.minAvailable | default 1 }}
  selector:
    matchLabels:
      {{- include "altius.selectorLabels" (dict "name" "security-service" "release" .Release.Name) | nindent 6 }}
{{- end }}
