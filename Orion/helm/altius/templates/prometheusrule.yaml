{{/*
HighErrorRate, HighLatency, and StorageUnhealthy alerts reference application-level
metrics (http_requests_total, http_request_duration_seconds_bucket,
altius_storage_healthy) exposed by prom-client on GET /metrics.
PodRestarting uses kube-state-metrics (available out of the box).
See values.yaml monitoring.prometheusRules.enabled (default: false).
*/}}
{{- if .Values.monitoring.prometheusRules.enabled }}
apiVersion: monitoring.coreos.com/v1
kind: PrometheusRule
metadata:
  name: {{ include "altius.fullname" . }}
  labels:
    {{- include "altius.labels" . | nindent 4 }}
spec:
  groups:
    - name: altius.rules
      rules:
        - alert: HighErrorRate
          expr: |
            sum(rate(http_requests_total{service="{{ include "altius.fullname" . }}-api-gateway", status=~"5.."}[5m]))
            / sum(rate(http_requests_total{service="{{ include "altius.fullname" . }}-api-gateway"}[5m]))
            > 0.01
          for: 5m
          labels:
            severity: warning
          annotations:
            summary: "High 5xx error rate on {{ include "altius.fullname" . }}"
            description: "More than 1% of requests returning 5xx for 5 minutes."
        - alert: HighLatency
          expr: |
            histogram_quantile(0.99, sum(rate(http_request_duration_seconds_bucket{service="{{ include "altius.fullname" . }}-api-gateway"}[5m])) by (le))
            > 2
          for: 5m
          labels:
            severity: warning
          annotations:
            summary: "High p99 latency on {{ include "altius.fullname" . }}"
            description: "p99 response time exceeds 2 seconds for 5 minutes."
        - alert: PodRestarting
          expr: |
            increase(kube_pod_container_status_restarts_total{namespace="{{ .Release.Namespace }}", pod=~"{{ include "altius.fullname" . }}.*"}[1h])
            > 3
          for: 0m
          labels:
            severity: critical
          annotations:
            summary: "Pod restarting frequently"
            description: "Pod {{ "{{ $labels.pod }}" }} has restarted more than 3 times in the last hour."
        - alert: StorageUnhealthy
          expr: |
            altius_storage_healthy == 0
          for: 2m
          labels:
            severity: critical
          annotations:
            summary: "Storage backend unhealthy"
            description: "PostgreSQL storage health check failing for 2 minutes."
        - alert: SyncRecordsFailing
          expr: |
            altius_sync_consecutive_failures >= 3
          for: 5m
          labels:
            severity: warning
          annotations:
            summary: "Sync datasource failing repeatedly"
            description: "{{ "{{ $labels.datasource }}" }} has failed {{ "{{ $value }}" }} consecutive poll ticks."
        - alert: SyncStale
          expr: |
            time() - altius_sync_last_processed_timestamp_seconds > 300
          for: 5m
          labels:
            severity: warning
          annotations:
            summary: "Sync datasource has not processed a record recently"
            description: "{{ "{{ $labels.datasource }}" }} last processed a record over 5 minutes ago."
{{- end }}
