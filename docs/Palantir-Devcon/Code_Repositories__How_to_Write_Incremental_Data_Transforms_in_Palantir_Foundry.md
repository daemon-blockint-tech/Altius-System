# Code Repositories | How to Write Incremental Data Transforms in Palantir Foundry

[Music] doing battery computations can be incredibly costly not only from a compute standpoint but also because it impacts your users like if you're recomputing multiple terabytes or petabytes of data it can take time and your users are going to be seeing stale data until your computation finishes so if you have a data set with multiple terabytes of data in it and you have 1 million euros why don't we just recompute the 1 million euros like the code didn't change so we can avoid recomputing the whole historical pipeline this will allow the computation to be done in seconds bringing new data to your users let me show you how you can compute incremental transforms within foundry so let's create a data set and upload some data that i have on an scv file within my computer for that i just need to drag it into a new dataset within foundry confirm that i'm importing that in accordance with the policy of my stack and hit upload once that file is uploaded i can see the rows in the data set on the interface so now that you have some data within foundry let's also create a repository here i'm going to jump you right into the end of the process you already have other videos showing this so let's just create some code to add a process timestamp into our data set so that we know when every builds run here we can see it run at 12 42. if we keep reheating build it's just gonna keep recomputing and that timestamp is just gonna keep incrementing into the last time i built here you can see that the build is at and we can see in the process timestamp that it's 12 46 as well now we want to explain incremental so let's go into it you can start by searching on the docs for incremental and it gives you a write-up of how it works technically it's just an annotation that you can put into your transform that you will detect if new transactions exist within your data set and compute those transactions on themselves are the files that exist within your data set here we can see that the sav file we added already exists there which represents those two columns now if you keep re updating the data with the file name being always the same what you're doing is actually generating what we call an append sorry an updated transaction and that update transaction just replaces the file every time a file is replaced you're invalidating the incrementality for that file in itself and you're going to ask for it to be recomputed so here because we updated the file with the same name we're generating an updated transaction which once built it's just going to rebuild the whole thing you see there is no new file on the files and if i hit build our app our snapshot timestamp in this case sorry our process timestamp is just gonna keep updating to the latest time here we can see again 1259 and we see on the process timestamp of 59 as well so let's create an actual new transaction we can do that by creating a new file called transactions.testcv2 in this case let's add another row of data and save it with a different name transactions2 dot suv and again just drag it to the interface and hit confirm like there are other ways to to ingest it into the into the platform but here we're just showing it with drags as you can see you were generating an update transaction sorry in this case an app and transaction which shows up here on the history each one of the transactions and it shows here as well one file extra in the data set so once we build um only the new transactions are actually gonna be processed so you can see here that the first three rows still have the old process timestamp and the new row that is from the new transaction was already processed at 1pm 3 minutes which is this row here selected so let's now invalidate a transaction in this case let's reupload a file with that already existed transactions.suv which are the first three rows we can delete the rows just upload them again go through the process generate a new update transaction import and validate like there are a million ways of interesting date into foundry we'll cover that in other videos so here you can see like because we've rebuilt we invalidated the the transaction that already existed it forced the incremental pipeline to recompute everything you can now see that the process timestamp all of them have the same time which is 1 pm 6 minutes so let's add some more data to show some edge cases within incrementality here we're adding a new row i accidentally skipped id number four but it's okay doesn't really matter we add the file called transaction three we're gonna drag into it again we're gonna generate a new append transaction which just adds an extra file we can confirm that within both the history and within the details the row itself doesn't show up in order but that's because when rendering a preview it just randomly loads the files so the order within the files is going to be retained either outside of the files it isn't but that's fine here you can see that only row only the new row actually got recomputed if you keep reheating build here you're just gonna end up getting always at the same time stamp because the data is not getting recomputed again we can also see this by adding a new column or some new logic and hitting rebuild because although that had already been computed the new logic is not going to be applied to any of the rows that had already been computed however if you keep ingesting data for example here we're going to add another row of data now transactions far this new data will already run the whole code and we'll already get that new row that we added in pi spark here again you can see that it's another pen transaction the file is there now has transactions for so if we hit build the column four bar that is going to continue being null to all the other columns now is present in this new row of data that we just updated as you can see here now what about if i want to actually recompute it in all the other data so in this case you need to recompute your whole your whole data set meaning that you need to bump uh the incrementality and say let's do a bill that is not incremental for once to do that we actually have a parameter in the incremental that we call semantic version that tracks when it was the last time that your whole code ran bumping semantic version which defaults to n will force your whole pipeline to be recomputed or in this case your whole data set is going to be recomputed and that new column that you just added is going to now show present in all transactions as you can see here after the build finishes so that's pretty much a tester of incrementality i'll there are some other parameters that you can explore in the documentation namely to verify the inputs are incremental and to do so and to play around with some more advanced things we may cover those in future videos so for now thank you for listening in and see in the next video [Music] you