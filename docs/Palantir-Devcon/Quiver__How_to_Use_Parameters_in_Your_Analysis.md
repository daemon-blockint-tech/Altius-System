# Quiver | How to Use Parameters in Your Analysis

[Music] how many times have you created an analysis and then you've been asked to recreate the same analysis again for for instance a different business unit or a different customer segment well often oftentimes what people are actually asking for in that case is an interactive dashboard and luckily corbin can help us with that in quiver you can simply turn your analyses into dashboards by means of incorporating interactive and well-defined components such as parameters into your analysis and once you've done that everyone can use your analysis as a dashboard so in this video we'll look into how to use parameters and how to create a dashboard out of an existing analysis let's go let's start with the airline selection parameter since we would like to be able to select the name of the airline in question we need to create a string based parameter we have the choice between two different string base parameter types property value select parameters and text parameters as for all quiver elements note the abbreviated descriptions of what each element does in the color bubbles for instance the property value select parameter takes an input object set the blue bubble on the left hand side and outputs a string the yellow bubble on the right hand side the text parameter however doesn't take an input but also outputs a string this means that the text parameter is essentially a free text field whereas the property value select parameter lets the user choose out of the unique and restricted set of property values of a given object which is why it requires an input object set but let's look at this in practice in this case we would like to create a drop down selection where we can choose only from the set of airline names that are actually viable analysis options and are thereby in our data so we configure the parameter to take the airlines as an input object set and use the object description property as the pre-populated values and drag it to the top of our canvas where we will want it to live on later on as well if we want to limit the selection to a single airline we also need to toggle the single select option finally we can configure the prompt and provide some helpful information next to the parameter to guide our user right now our airline selection works via the table that we can see above the newly created parameter let's examine how this works more closely in the dependency graph such that we can replace the table selection with our parameter to filter down our object set as we can see the table sits in the middle between the input object sets of all airlines at the top and the select single object at the bottom now let's create the same single object but using the parameter to filter our input data set we will filter on the airline object where the description is equal to the parameter so we first select our condition and then we select our predefined parameter to match the condition and filter down our object set let's now rename our object set to selected airline when zooming out on our dependency graph we can now see that the filtered object set has been created but it is not yet tied in with the rest of the analysis so let's now go ahead and change the input of the downstream objects to our newly filtered selected airline object set essentially we will insert the new object set in the place of the old one by transferring over the dependencies we can simply do this by changing the root object set for all three dependencies now let's take it for a spin and see if it works great news when selecting a new airline in the drop down all downstream charts indeed update we can now clean up the two analysis elements that we are not using anymore in order to do that you can just delete the topmost element and all of its descend elements will be deleted as well note that this delete is a real delete so it doesn't just hide things from view but it actually deletes all of the cards that we've created let's now return to the canvas view so now we have a drop-down parameter that controls all of our charts by selecting the airline from a drop-down list however we would like to add another parameter here and be able to specify the date range for the flights that have been cancelled for this purpose we can add another parameter to our chart because we would like to add a time range and not just select a single date we need to choose this option for the parameter as well let's reorganize our view again and add the parameter to the top of our canvas in the same way that we use the airline selection parameter to filter down the airline object set we should now use the object set that is highest in the dependency graph and that also has a date property that we can filter on it is important to take the object set that is highest in the dependency graph such that we don't have to duplicate our work downstream in our case this is the flights object set so essentially the same way that we showed above we repeat the process we filter on the date by means of our parameter and we transfer the dependencies from the old input to our newly filtered output now when we switch back to the canvas view we can test it out and filter our analysis to for example only flights between february and august great now our analysis has been transformed into a small dashboard and it can now be used by more non-technical peers to track flight cancellations for different airlines and different time ranges just by clicking and interacting with the parameters perfect now you know how to use parameters in your analysis and turn it into a dashboard so over the course of the past couple of videos we've given you a very basic insight into how to use quiver there's so much more that you can do with it so in our next videos we'll show you for instance how to add time series data to your analysis time series data that might be coming from machine sensors for instance and also how to use more complex transform tables and formulas and even models on your data see you there [Music] you