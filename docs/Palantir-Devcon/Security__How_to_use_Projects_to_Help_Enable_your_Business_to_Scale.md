# Security | How to use Projects to Help Enable your Business to Scale

[Music] thank you have you ever wondered what the best project setup is for a production data pipeline in this video we will walk through the project setup that will enable your business to get access to the data they need the fastest while also keeping the necessary data Protections in place our recommended setup will likely be the easiest to manage as your business grows for illustrative purposes we have expanded components of the aviation reference project into multiple projects now let's get started in order to follow along this video you will need to have the permission to create projects this is a permission typically included in the editor and owner roles which you can check in the platform settings shown here and these editor and owner roles must be granted in the namespace settings shown here if you don't have access to roles or namespace settings you should ask your Foundry administrator about getting permissions to create projects assuming that you have the permissions to create a project let's discuss the anatomy of a pipeline that can support a more modular permissioning setup a key part of Designing a data Foundation is deciding which projects need to be created for your production data pipeline in order to enable future expansion and easy permission management the typical pipeline stages include data flowing through four projects that have distinct purposes first there is a data source project where ingested data will land and be cleaned second there is a transform project where data will be joined to produce reusable data sets third there's an ontology project where data will be used to create discrete business objects and fourth there is a workflow project where the business assets live in our case this is an operational flight alert inbox application these different projects help delineate responsibilities between teams and they provide meaningful and manageable boundaries for permissions as an example let's create one of these projects from scratch so we can see the steps we took to build this pipeline we'll create only the transform project in our example returning to the projects page we can click on the green new button and pick project depending on your Foundry instance you might see different dialogues for creating a project to start we can input a project name a description and pick a namespace as a reminder a namespace is a security primitive that allows an administrator to Define common settings across a group of projects in our example we will pick the sky Industries namespace we will then restrict access to only the sky Industries organization lastly we'll set the default role to discover so that all users in the sky Industries organization can discover the project in the existence of the resources inside the project if a user is interested in data access they can then request access to the project once we've completed the dialog we can then click create after creating the project we will want to make it easy to manage different levels of access to the project going forward to do that we recommend creating a group for each role except for the default rule managing permissions via group membership will make it easier to onboard and off-board colleagues in our example we will create three groups one for owner one for editor and one for viewer we can do this in the platform settings interface as shown after we've created the necessary groups you can go ahead and Grant each of these groups their corresponding roles in the project also in each project we'll create a code repository which is where our Transformations will live now that we have a project to build our pipeline in we can start using Foundry to author a business logic for details on best practices review the code repository videos my colleagues made before we can use data from another project we need to create a reference to it in our current project once we do so we can use that data set in our builds and enable anyone in our project to author Transformations on that data in our example we can see what it looks like when we add a project reference to the flights data set in data source project to our transform project that completes our example of creating the transform project from scratch let's now return to our completed pipeline if we toggle the data lineage Legend to project we can see how all the data flows between the projects in our pipeline the benefit of having our pipeline split up into four projects is that we can give users specific Rolexes separately for each project while also keeping project access uniform for example we will add our first business user Eric Yates to the aviation ontology viewer Group which will give him viewer access to the aviation ontology project switching the data lineage Legend to permission mode we can see that Eric now has viewable access on the ontology data sets which are the blue nodes given all the project defaults are set to discover Eric only has discoverable access on the data source and transform projects which are purple nodes note that having Discoverer access on the data source project in a transform project will not preclude users from being granted access to Downstream data let's switch to another more complex example let's say Eric's colleague Linda is someone who needs to own and maintain their production pipelines that generate the intology data but she does not need to have the ability to manage permissions on the ontology data herself to support this setup we would add Linda to the owner groups on the data source and transform projects into the editor group on the IntelliJ project switching out permission mode to view as Linda we can see that data sets Linda is now the owner and editor of across the project boundaries in conclusion taking the time to deliberately think through how you want to structure your projects is important to allow your work to scale faster in managing permissions via group membership makes it easier to onboard and off-board colleagues with different roles across your organization [Music]