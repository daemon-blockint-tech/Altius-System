# Code Repositories | Reviewing Code and Best Practices

[Music] users of all technical backgrounds can find value in foundry for many use cases yet at times they are still responsible for writing readable admin tempo codes foundry makes it very easy to get started with pi spark and data pipeline development and that's a great one for productivity yet in my many years of experience as a data engineer i find that people punch the same pitfalls time and time again not sure though it's very easy to avoid those traps once you know what you're looking out for join me for a quick review of a verbal story in the foundry reference project and i'll show you what i mean to review a transform we need to first understand the transform's intended purpose to do so we first look at inputs and outputs here we have the primary input which is a data set of flight instances followed by a number of lookup tables then upwards and runways which are used to enrich the main dataset with foreign properties the output is also a data set of light instances blood cleans up and reached for optimal comprehension to make far easier manipulation downstream we parse out times into native parsepark timestamps we do this by applying a couple of ugfs defined as closures finally this last step to some denomination by bringing in runway metadata directly to the flight rows which is how downstream operations expect them to be to start with the refactoring i want to first improve this long line here long lines like these are bad for readability and make mistakes more likely to happen let's create a new branch and break up the line by taking advantage of implicit continuation by enclosing it in parenthesis let's remove the linear suppression now the lint is flagging at this line is using the inner operation which is considered an anti-pattern we should use union by name instead which provides a bit more safety by enforcing the column names line up correctly another thing we can prove here is how renaming takes place since the final name of this column we're assembling is going to be airport we should use that directly for better clarity of intent notice how we even save up one line of code with this change next i want to move these definitions down below whether i used code locality helps comprehension since these values are only used in a single place there is no reason for them to be defined here separately you may argue that this makes the local codes more complex which is true but as the zen of python goes explicit is better than implicit this is an important operation it creates our primary key and we should make it extra clear what columns are being used for it and how i will also move this line down below a similar code to locality reasons the next change i want to make is how this ugf is being declared using the decorator variation makes ugf declarations more local and explicit it also simplifies the code a bit the same goes for the other udf moving on to this segment there are some style adjustments i strongly recommend use keyword arguments to make the chain operation more readable and predictable it also helps avoid common mistakes people often forget that the default joint type zenina join so it pays off to make it explicit in the next line i noticed that there are a couple of necessary casts all this is doing is dropping some precision from the data which is not going to help with the geolocation let's remove that step now back to the udf's refactoring i remember that i need to fix some lines that are using the old names now back to the first udf i decided to move it out of the main transform body although this aggregate reduces locality it's a good trade-off in this case as it makes it clear that these udfs have no dependencies in local variables they are pure functions it also declutters the main body of codes a bit there are some tweaks worth doing today bodies here i'm applying a programming pattern called early return which helps with code comprehension there are also some redundant arguments which once removed make the call fit in one line otherwise this function looks fine with the other one i decided to change in bar extensively it's better to return no values instead of dummy values with this change the body becomes significantly simpler this doesn't change the overall behavior of the transform as the dummy values were just ignored same as those next thing i want to change is this segment it's essentially doing convoluted standing for during operation it sends all the data to the driver in the form of a vendor's diaphragm then converts that into a map column which essentially broadcasts the same data back to spark at this scale it doesn't impact performance too much but it's not good practice and honestly it's a bit hard to understand so let's replace it all with a couple of joints which are easier to comprehend first i'm creating a mapping dataframe from the origin airports it's just the airpods diaphragm with some columns renamed joining into this with the right key is equivalent to mapping values notice how the resulting code is simpler and more straightforward we can do the same for the destination app words in these next lines i'm also tweaking the join syntax to make them more explicit then i'm moving this whole block to the return statement as there is no reason for it to be separate this is an optional step there are different schools of thought when it comes to long chains of operations like these but i personally prefer to indicate logical segments by using white space rather than break up the chains into separate segments this makes the code more declarative and the data flow more explicit but the important part is keeping related code together and chaining is just one way to achieve that alright this code's already looking much more friendly next i'll tweak how runways is joining to the main data frame there is an unnecessary name for each design it's good practice to use natural joins when possible as it avoids creating duplicated columns in the resulting data frame the delay group joins can also be simplified a bit in general it's good practice to clean up data frames prior to joining them to others not afterwards this is good for locality as it is harder after joins to follow which columns come from which data frame back to the top first i'm removing some imports that are no longer necessary than removing the spark profile override i did some testing of screen and verified that the default amount of executable memory is more than sufficient for this transform as long as it allows spark to use broadcast joins since this method is disabled on the stack by default i'm asking spark to use it explicitly by adding hinge to the secondary their frames we're journeying to the main one note that this only works as long as the broadcasted their frames are fairly small which is the case here since they are effectively just look up tables and we're pretty much done with adjustments to improve this transform now i'm running preview to test the codes looks like there is a problem at line 30. looking closely the issue is clear the alias is not being applied on the column it's on the diaphragm itself this worked originally by coincidence because we are using a straight union instead of unit by name and since there is just one column the names didn't actually matter but the code was incorrect how long it's an easy fix great now the preview worked we can run a full build for further validation the build completed and the data looks fine but i'd like to point out a few other things in this transform that should be considered called smells first one is the use of dropping a used like this it will drop any rules that have a no in any column even though we are only using a few columns from the original data frame this line will cause the unused columns to also interfere with our output this can lead to unpredictable and hard to debug issues also down here i recommend against using field na nodes are generally a better indicator for missing values with better semantics in spark operators finally this comcast approach to construct the primary key is rather brittle any nodes in one of the columns will cause the entire expression to evaluate you know which will lead to uniqueness issues i won't touch these entire patterns as it would cause a semantic break in the transforms output and possibly reach downstream but it's worth keeping these problems in mind when designing the transforms that's it for now i hope this has given you some idea of how my fellow date engineers and i maintain a high standard for our transforms gold it's all about keeping the quotes explicit contextual and readable and there's a few tricks that help with all that only practice makes perfect but this should be a good starting point happy transforming [Music] you