# Spark Basics | Shuffling

[Music] thank you I've cut compute costs for some transforms by over 50 percent after making some very small code changes I'm talking 10 lines or less that may sound impressive but it actually only takes understanding two things first you need to know about shuffling and Spark then you'll want to learn how to interpret a spark query plan and identify where unnecessary shuffling is happening during your job after watching this video you'll have the Essential Knowledge you need to start cutting compute costs for your own organization shuffling is how spark redistributes data across partitions in this example we start with pet data split into three partitions being processed on two executors a group by operation on the pet type key is called which triggers a shuffle the data is exchanged to form four new partitions for the executor to run Transformations on shuffling generally needs to happen for common Transformations like group by join repartition and distinct so shuffling is nearly inevitable for spark applications unfortunately shuffles are very expensive for two primary reasons the first is that potentially very large amounts of data have to be transferred over the network additionally spark employees in-memory data structures to reorganize records before transferring them which takes compute power and memory since shuffling is so expensive it's vital to minimize the number of shuffles that occur during a job now I'm going to show how to determine the amount of shuffling happening during your job and a simple adjustment that can be made to reduce shuffling in this transform I'm wanting to bring four data sets together starting with the flights data I am left joining the delays data on the flight ID key then left joining the origin airport data on the origin key and finally left joining flight alerts on the flight ID key let's go ahead and run this so we can take a look at how spark interprets this code the job took about 30 minutes to run in order to see why it took so long we're going to want to check out the spark details here we can see that the aggregate runtime or the sum of all task durations is nearly three hours that's probably largely due to having to shuffle 82 gigabytes of data between stages that probably also contributed to the amount of disk spillage that we see in this job to dig in more let's go to the query plan the graph version of the query plan is a great way to see what spark operations are happening during your job so here is our flights input and here is our delays input the data sets are then joined together and you can see the type of joined and metrics about the resulting data frame in this next block for a join to happen both data frames need to be partitioned in the same way in this case that requires re-partitioning both inputs on the join key flight ID the 200 that you see in the hash partition function is the default number of partitions set by Spark the next join is on the origin key so the airport data frame has to be repartitioned as well as the flights plus delays data frame last the flight alerts data is joined on causing re-partitioning back to the flight ID key each time this re-partitioning happens you'll see the word Exchange exchange is another word for shuffling you can see that there are six exchanges in this query plan you might have noticed that the data frame being joined onto has two exchanges on flight ID we can use this information to improve our transform so let's go ahead and make a branch to create this change the change is simply joining the two data frames using the flight ID key together one after the other so we're going to want our origin airport data frame to be joined on first note that it is important to ensure that changing the ordering of joins won't affect the logic because gaining efficiency is not worth losing correctness let's see what happens when we run this transform right off the bat we see that this build is just nine minutes where it was previously 30. let's dig into the spark details here we can see that the aggregate runtime is one third of what it previously was and similarly the amount of data that's shuffled between stages is about a third of what it was and there is no disk spillage anymore let's go ahead and check out the query plan to see what's changed there you can see that there is one less exchange than there was before we only need to re-partition our main joining data set on flight ID once since it's already partitioned on flight ID and has 200 partitions it does not need to shuffle again looking at query plans carefully like this can help you identify places to improve your code by reducing the amount of expensive Shuffles foreign