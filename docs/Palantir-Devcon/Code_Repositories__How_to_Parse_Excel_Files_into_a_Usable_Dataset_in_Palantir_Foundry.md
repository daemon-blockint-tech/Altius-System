# Code Repositories | How to Parse Excel Files into a Usable Dataset in Palantir Foundry

[Music] at the beginning of every foundry pipeline is raw data as a foundry developer one of the most important skills you'll use is converting raw data files into foundry data sets at the beginning of your pipelines this will be especially important if you're working with unusual or complex file types that aren't automatically parsed by foundry today i'll be showing you how to parse one such file type that requires some extra logic the ubiquitous and ever popular excel file in doing so we'll explore how to read from the foundry file system to parse files of all different shapes and sizes let's get started here's a folder i created for our excel data parsing tutorial right now it just has a code repository inside that i initialized for our code first let's import an excel file called colors1 into foundry using drag and drop functionality we'll select upload to a dataset without a schema which holds a collection of arbitrary files as you can see our new dataset contains colors1.xlsx which is the file we started with i'll rename the dataset to favorite colors for ease now we'll open our code repository to begin the code to transform this rock cell file into a foundry data set if you haven't watched our video called how to write data transformations in palantir foundry you can do that now as a precursor to this video the link is in the show notes for our code instead of using foundry's transform df we will switch to foundries transform transform df converts a data set into another data set while transform converts raw files into a data set to use transform we just have to define an output variable and define our input data set which i'll do using the rid of our favorite colors data set now we pass our source out and the foundry spark context ctx to our transform we'll import some necessary packages to our project the row object from pi spark dot sql the string struct and struct field types from pi spark.sql.types and xlrd and excel reader package now that we've done our imports and cleaned up our transform let's dig into the code first we'll define a variable file status that holds a list of the different files in our source just a note to use globe to select which file types you want here we only have an xlsx file but in many raw file collections you'll have a variety of file types that you'll want to filter out for your transform we'll just assert that the length of file status is 1 to make sure we polled our file successfully then we'll define latest file to be the first element of file status which in our case is the only file if you had multiple files you'd just wrap the rest of the code you were writing in a for loop for each file we'll define an empty list of rows which will hold the intermediate contents of our excel sheet now we're ready to open our source file system and read our file from it by using latestfile.path for non-text files you'll use rb to read as binary i'll define our workbook by using xlrd to open our workbook like this since excel files can have multiple sheets i need to define which sheets we want to read from in this case we only have one sheet called sheet1 we'll grab the headers from this sheet by using row value 0 to get the first row we'll process the rest of the rows in a for loop we'll use a variable excel row to hold each row for now then we'll use the row object we imported above to generate a zipped up copy of the header and our current row and appended to the list of rows we defined above for our data frame the row object is recognized by pi spark and will allow us to automatically convert to a data frame later now that we've read in all of our rows from our excel sheet we just have to create the schema for our data frame a schema in pi spark is a struct type consisting of struct fields the colors one sheet has three columns which are name age and color so i'll generate a schema with all three represented representative strings finally we want to write out a data frame containing all of this information we'll invoke our input variable out and use write data frame to write to it we also need to convert our schema and rows into a data frame first we'll invoke our spark session using ctx.spark section to create a data frame which takes in rows and schema as input great now we're finished with our code i'll go ahead and press build to generate our final output after the build you see that colors 1 is now formatted as a foundry data set thanks so much for watching and check out the rest of our foundry developers content for more tutorials and tips on engineering and foundry [Music] you