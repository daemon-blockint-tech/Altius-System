# Code Repositories | How to Unit Test PySpark in Palantir Foundry

[Music] independently if you're developing pipelines or writing software unit tests it's always one of the main software development practices that everybody agrees that it's a good idea to have so let's go into altering into a pipeline and let me show you how you can write a unit test within your pipeline to test the codes that you write inside foundry we start with the foundry reference project let's open the data source and add a unit test to it we can go with the flight control system once we open it we can see there is a repository inside opening the repository exposes both python and sql transforms we're interested in the python transforms so let's open it and we can see that there is a collection of pipeline code here but there aren't any tests we can also observe that master is locked so let's create a branch so that you can push some code in let's call our branch add unit tests we hit create and once the branch is created we can now start doing some setup we can start by going to build gradle and see if the plugin that runs by test is active in this repo it was already active sometimes it's commented out so you should come here and then comment it we should also create a package called test in the source folder so that we can have a place that hosts our test that pipe test will recognize within it let's create a new file we're using pi test nomenclature which is test underscore stem name here we're going to call it example but please give it a name that's normally meaningful to you we created and because it's a python file it comes with a damn template transform we don't need it so let's delete it and let's create a test using pi test nomenclature as well where we'll be asserting true against false now this assertion is gonna fail for sure but let me show you the test failing after all assert test equals false it's always gonna fail and then here we see the ci running and once the ci finished we can see that the test failed opening the details shows us that it was expecting true but it actually got false which is the correct result on this test you can also hit play if you want to run the tests here which doesn't require you to wait for the ci checks and it's often faster however this test is not very meaningful after all we're just comparing true and false let's actually add some a pipeline test something that touches pi spark let's go into the flights uh pipeline codes and let's select the random line for example line 34 here has a bit of pi test of pi spark code that we can use for to write a test run so let's copy it and let's create a new function where we can call it mark null tail numbers which returns the resulting data frame from that expression so here it is it receives a data frame it does a few nodes with an a and returns at the frame afterwards we can import the the function from flights where you can just say from my project add setting reach that flight's imported and pause the video here if you want to read the code we press the spark session and we assert if the tail number is a string i'd like to note that we're using the keyword is which in python actually compares references doesn't compare the content of the string and because of that it's actually making the test fail so because a developer may often miss a detail like this we actually have a green line which we can see here once mousing over it gives you a warning please use equals to compare string literals or bytes or tuples etc and the warnings on the bottom also warn you about it using string equals actually compares the content of the strings not the references which will make this test pass you should always pay attention to the linter well let's also remove the test that was meaningless and commit and now we should have the tests passing here we go we now have the tests passing if you go back to the code you'll see that we were passing a fixture called called spark context it gives us the access to the spark context created data frame and here we can now finish this with a pull request where we say add pipeline test create let someone review it and merge it and that's it thank you for listening [Music] you