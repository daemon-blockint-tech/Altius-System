# Developer Deskside | Building Apps on Kafka Streaming Data in Palantir Foundry

[Music] thank you have you ever wanted to use streaming data from your manufacturing plant to detect problems with machines and then support your teams in collaborating to fix those issues well today we're going to do just that with Panther Foundry we're first going to adjust the streaming data from Kafka we're then going to build a streaming pipeline using pipeline Builder and then going to construct an ontology on top of the ontology we're going to build a low code application using workshop and then we're going to build a fully custom react application using The Foundry apis let's jump into it so by the end of this project where will you be well well I've gone into Foundry will have built this Kafka machines monitoring project inside here we can see we've got a streaming data set of our metrics so for example for each of our different machines in the factory we can see their current production rate and their current temperature and see when that metric was Etc we then build an ontology out of this and build a low code Workshop application where we can see free trial machines how they're doing so we can click on any machine and see kind of what section of the factory it is in and see the performance over time and then if there's some issue we can then report an issue um explain what the issue is and that gets created as a maintenance request so something is wrong um you know sort that off and then we can then manage these maintenance requests in Workshop so we can see there's something wrong uh requests that we just submitted but then also we will build a fully custom react application which if we refresh we'll be able to see our new maintenance request that we just submitted and in here the maintenance team can say uh their their resolution so no there isn't um click Mark is resolved um and then if we go back to our workshop and reload uh we'll be able to see their comments back here so in our maintenance request something wrong we can see now this has been closed and it's updated with the comment that we just put in and that's powered uh we'll go through writing all the code um in typescript of how we built this um as well as all the things we do in boundary to ingest the Kafka data to pipeline it with pipeline Builder to construct the ontology in the authority management app um build the workshop application uh build the actions everything welcome back we've now got a computer in front of us and we can start getting to to the work um for some more context on what the problem is we've got this Factory where there's a number of sectors so we've got sectors a b c and d and actually split into two different buildings maybe these are miles apart this Factory is very very large you know it takes you know an hour to walk uh all the way around it but currently they have to patrol all the way around it looking at the machines inspecting each one but recently they've gone through some digital transformation they have bought new Internet of Things sensors that they've slapped from the machines and they're recording maybe production rate or um temperature these kind of metrics uh but we aren't doing anything with those they're being put into a Kafka stream but it's not being used yet and so what we want to use is Foundry pound to Foundry to process these metrics and build some kind of dashboard where they can automate this thing where they can actually look at the machines remotely as well as maybe you know collaborate between teams in setting out the right maintenance Crews to go and fix them so let's try doing that so the first step we've got to do is actually get that data inside Foundry and the way we're going to do that is using data connection so first I'm going to log in and I've logged in this is Foundry right on the left we see the applications I've got data connection just here if you don't you can click view all and you'll be able to find data connection and we're going to create a data connection agent so I'm going to go to the agents Tab and hit new agent great it walks us through this uh so maybe you don't need to watch me do it but um I'm going to do it anyways so I just read these um so we see reporting on a Linux machine I've already got a Linux virtual machine set up in the cloud for me um and so we need to make sure that that Linux machine is able to send data to Foundry again I've made sure those those firewalls are set up next we'll give this a name I'm going to call this my agent for Kafka because that's what it is uh and we're also going to create a new project for our work so I'm going to call this um Kafka machines uh monitoring and I'm going to put it in the devrelation's internal organization default role of viewer is probably okay um for this project it depends what your permissions are you probably want to check those um in this case I'm going to allow everyone to view the data inside this project but only I would have owner and then we could change that maybe to a group later or we can remove that default viewer access later that's fine continue um and we're going to click this generate one time you slink copy that I'm going to open up a terminal and I'm going to SSH into my agent I've said it was SSH configuration so I can just hop right in like that um next I'm going to copy this command that I just did and I'm going to paste that in and you see what this is doing is it's downloading a agent from the stack we're going to wait for that to download generally doesn't take too long uh it depends on your internet connection but usually this takes about 20 seconds so we're about 15 now so hopefully not long there you go 18. oh that's pretty close next I'm going to copy these commands to extract it I'm going to slide this over here uh we're going to extract the agent into a folder and then open that folder we don't have a proxy in this case but if you did have a proxy you might need to configure that in the configuration as well and then we're going to copy this command down here to start the agent so that'll start the agent on our Linux machine and I'll get it going and then I'm going to hit continue and hopefully uh in about five minutes time this should go green and we should be ready to use it okay I'm back it's been a couple minutes and we've got a nice green check mark so we're good to continue let's say click this get started thing and then on this screen we can see there's a Kafka Source before I connect to the Kafka Source I need to actually have one uh in the real world you've probably already got one where you're going to get connected to it but just so you can follow along in case you don't I'm going to follow the quick start for Kafka and you can see how that works so I'm going to find the quick stuff Kafka and down here is the Apache Kafka Quick Start um what we're going to do is download Kafka and in this case I'm going to put it on the same Linux virtual machine but it could be on a different host uh you'll just have to know the connection details and you'll see those later [Music] good thing here um I'm gonna go up and download that you see that's downloading next we are going to extract that and open up the directory and I'm going to use zookeeper for this you could use craft it's all all kind of the same and then we're also going to have another connection where we start the broker um so you open up that file and install the broker there we go I also know uh we're going to want to create some data so I'm going to open a new channel as well um so here we have to kind of create a topic and then write some events so that we have some kind of simulated events to work with I'm going to create a python file using the python Kafka Library so how am I going to do that well first I'm going to install um Kafka python Library and you see I've actually already installed it but you know that would install it for you and then I'm going to create a Bruiser so uh yeah Nano producer .py so I'm going to open up the nanotext editor for this created this file at producer.py python script I've actually got it here and I'm going to copy and paste this in but we've got a Kafka producer that connects to our cluster we've got this function generate fake metrics that generates these kind of random numbers for us effectively um oh we're going to pass in a machine ID a temperature production rate and the current time and we can see if you know the ninth machine is going to be off the 23rd machine is going to be overheating and the rest are just going to be kind of in a random healthy range or something like that and then we're just forever we're going to keep generating these metrics and then send them into that topic for the machine Matrix topic and then every five seconds we're going to just keep doing that forever so I'm gonna copy and paste that in as icon producer and then when you run producer hopefully we should see there we go in the background we saw it kind of creating a new topic for our machine metrics and every five seconds we'll be sending in new generated metrics then going back to Foundry we can then create our Kafka source so I'm going to go into Kafka again it leads you through the process um but I'm going to show you how to do it anyways we're going to connect fire and agent the agent that we kind of set up already um we'll call this Source R um machine Matrix Kafka cluster and choose a folder which is going to be in our project we created our Kafka machines monitoring project we can select this and then create a source again we're going to select our agent for Kafka and you can see it says Needs restart that means it hasn't already got the kind of right libraries to talk with kafik yet but we can automatically push those libraries from Foundry just by selecting it right here and then when we hit save and restart it'll have those libraries installed for the URLs we're just going to use localhost 9092 because we created the cluster on the same point if it was on a different remote host you just type in the URL for your Kafka government cluster somewhere else also in this case we don't have any particular authentication on our cluster we're you know only on this VM it's only accessible there but in the real world you probably want to have some kind of username password thing Simplicity we're going to skip that for now so we've just hit save and restart and now we're going to wait for that to restart it generally will take a few minutes um but hopefully uh not too long then soon we'll be able to explore and uh see what our metrics are like so it's a couple minutes later and now my page is automatically refreshed to show me all the different topics in my Kafka cluster so in this case we just have one topic the machine metrics topic so you can click into it to get a bit of a preview of what those records look like and this is what we're kind of expecting right we were sending in these Json records so we've got machine ID temperature production rate and time um which is exactly what we expect so that looks good I'm going to go ahead and create a streaming sync and so streaming sync is where all the records we kind of coming in live and flowing through uh which is exactly uh what we want and so do we want just getting in future records or do you want like right from the beginning in this case I'm gonna go right from the beginning so we get everything there um we're also going to say this fine as a normal flow and also putting it in the Kafka machines monitoring project in a raw folder is also also works and they've already figured out the schema for us excellent so I'm going to hit uh create sync and I get this pop-up saying running one sync um so I can hit open build details we can kind of see how how that is going um and overall we should see over time like this kind of continuing to pull new records through um as it kind of progresses through the build excellent we'll uh open the output data set of that stream and hopefully we should should be able to see those records coming in there we go this is exactly what I was hoping to see we have all these records coming in but you can see this isn't really ideal right we've got it as we've got this you know key field that we don't use and all the values are just this binary uh kind of string what we'd like to do is pass this uh and so the way we can do that is we can use pipeline Builder to build a pipeline so in the next video that's what we're going to be looking at we're going to be taking this input data and transforming it through pipeline Builder welcome back to this developer's site where we look at building a streaming pipeline connecting to Kafka constructing technology building a custom react app we're doing all the amazing things to say in the last video we looked at setting up a library agent connecting to a cafe cluster and bringing that into Foundry now we're going to look at how to transform that data and get it into the ontology so I can see I've got the streaming data coming in right live as these records are kind of being added what I can do here is just hit transform and then we'll be able to build a pipeline in pipeline Builder and transfer I'm also going to make this a bit bigger we don't need to see the tunnels anymore great um you call it new pipeline uh we can probably rename it later we want to create a streaming pipeline exactly so let's name this to our metrics cleaning cleaning pipeline and so we have our machine metrics coming in we can see kind of values there so we want to First transform this we can see the value is actually a binary column so we first want to cast that to a string and then we're going to pass that as Json so we're going to say the value we're going to cast that to a string and then we're going to parse the Json um plant loads in a second um we can copy an example Json paste it in and it's going to figure out the schema we're going to say we need to pass the value column uh that looks good I'm going to hit apply so part of the Json uh it doesn't look very different because when we have spark it kind of formatted almost a Json anyways so it looks the same but it is now paused uh to prove that we can flatten the struct as it's kind of a recommended thing so we're going to say we want to flatten the value column um that looks good to me and now we have all of these that looks good and we can probably drop the value in key columns now right so I'm going to drop the value and key columns because I'm not really using them um that looks good also it's kind of convention in day sets uh in Foundry to use snake case and so there's a handful handy thing here where you can normalize column names and apply a snake case to everything so you don't have to like rename each one uh the cool Turf Island from someone uh just last week actually that looks good our time we'd prefer an actual time stamp so we can uh cast our time to a timestamp um I think this is the second thing but if it's wrong we can just pop in and change that again no that looks right perfect so that's a good kind of pausing of our metrics and they're going to be flowing through we can go back to our graph and see that looks right we can call this on cleaning and if you wanted to we could add an output right here what I'm going to do now actually is going to join in another table so we've got these machine IDs but maybe really nice if they actually have names uh so what we can do is we could pause data from somewhere else like we could pull it from postgres or maybe there's another Kafka cluster for that or something else I'm gonna actually cheat a bit here and use our spreadsheeting tool called Fusion and so in this you can build custom tables just by writing them basically into a spreadsheet so let's go and do that so I'm going to go back to our projects and go to Kafka machines monitoring and here I can create a new Fusion sheet takes a second to create and then we can name this something like um machines backing fusion and so what we're going to use this for is creating a backing data set for our machines and so what does the machine have uh well it might have machine ID and we also said we're going to have like a name and we can just call that name and we have like an installed update um and the sector it was in right remember when we had that kind of drawing of our Factory we had different sectors so we might want to know where where the machines are so you can say machine ID I think we went up to what we can have a look at our metrics um yeah we have m0 to M24 so let's let's do those um zero M1 M2 and three and four and five uh up to 24. we can fill that in I'm going to call it machine zero machine one but maybe these would be you know more human friendly names like this is the the boiler or what what kind whatever kind of machine is going on and then install that we can also set uh is a date and time um and we could say maybe this one is installed in 2020 uh 601 at 11.

damn right um are we gonna have these date and times and then this is uh sector a 's rules x-ray uh these are some sector B Etc so I'll fill in the spreadsheet and I'll be back in a minute so I'm back I filled up my table and now we can sync this to a data set so I'm going to select my table and I'm going to go table new table uh we can call this machines and we're going to sync it to a data set and give that a minute we can actually click this little text over here and just check all the types to write so I'm going to set that to string string timestamp and string um I'm also just gonna rename this just remove the name because I don't actually know so there's just gonna be machines that looks great the other thing we mentioned is that we're going to set up kind of Maintenance requests so we could send tickets to the support team Etc and so we can create a basic schema in Fusion here if you watch Logan's video linked below um you can see how to set this up in pi spark and do it kind of more properly but you can also create it quickly in Fusion so I'll show you how to do that here just for kind of a bit of uh kind of a different way of doing it um so very similar we might have maintenance request ID probably spelled that wrong or have a title uh that kind of request comment uh the resolution comment um and then maybe the machine ID that it relates to uh who is created by and the current status and so we don't actually need to fill anything in for these columns because we're going to be adding to this table all through right back which we'll get to uh when we create the ontology but we'll um set up that for now I'm just going to add a little bit of formatting just so we have a nice bold titles um and here we can create a new table we're going to call this one uh don't know what you call that maintenance requests I'm going to sync that as well there you go and that's loaded now and we'll have data set name maintenance requests and again just set up the uh types here it's probably more important here because I can't infer them because you don't have any data um I think these might Always Be Strings though uh as they are yeah maybe we also want to uh we'll have to create that column as well obviously we have a bit of a change and you can also see how to change the kind of ranges so we're going to update that to M1 as well um hit save and then that's fine and hit save again great um oh the trick I'm doing here to fill these out is you if you kind of select one you can press Tab and shift tab to go back and forwards and then just type the first letter of the kind of drop down value so s for string or t for timestamp and you can change them quickly like that if you want um so it's kind of cool um yeah anyways good uh so we've got R2 data settings set up and we can go back to our pipeline and now we should be able to join in the kind of machines and we can pull in say you know the title or something like that and and build a better title so the way we can do that back in our pipeline is we can hit add data add those sets from Foundry and find that machine stage set so there it is that um and you can see that is built it'll take a second preview but um we've got kind of exactly what we're expecting um and now we're going to join it with our cleaned version um and what do we want uh we want to keep the original idea there um and we're just going to take the machine name that's good I'm gonna join on machine ID great I'm going to hit apply and we should get that joined and go back to my graph and see what that looks like so after the joining this is exactly what we're kind of expecting we've got machine ID and now we've also got machine name what I'm going to do next is maybe make a title for this metric subject um so I'm going to create another transform and we can concatenate some of the strings right so we can create uh title where we don't necessarily need a separator but we do want say the name so the machine name and we can have a kind of const value of uh metrics at and then the timestamp right so column we actually want to cast the time column to a string and or like that that's clever it gives you a little raw formatter let's try just the default and call this um title and see what that looks like that's why the preview is really useful and then being able to quickly uh see what these things look like it's good machine 18 metrics yeah that looks good I think I might uh tweak the format um just so we don't have to have nanoseconds because we know they're only coming any every five seconds um so let's go for yeah holiday and then seconds so let's give that a go and see how that looks like okay that looks perfect that's kind of exactly what we want and we can also drop this name column now so we're going to drop the themecon brilliant done applying these changes and this is going to be uh I don't know adding title let's see what this transform does and finally uh we're going to add an output so this is where it's going to save it back to a data set where I'll be kind of updating it through streaming all the time and we'll call this uh machine metrics clean and that all looks good and now I'm going to deploy my pipeline which is going to kind of kick off stream build and then we'll start seeing the data flowing through so we're going to hit deploy pipeline to load so I hit deploy and now I've got this pop-up successfully deployed pipeline I can then view that Pipeline and see that kind of uh building through and you can see it's already kind of started up a bunch of different things okay I'm back now and our pipeline is running I can see this looks exactly as we expect we've got our kind of title in there we've got all the fields passed um and now our next step is to get this our machines and also our maintenance requests into the ontology so where we're going to do that is we're going into the ontology manager on the side um so again you pop up from this and view all and then ontology manager if you don't um already have that there it's going to open up the ontology manager I can close the other tab so you don't need any more and first off we're going to build our object types then we're going to build some links between them so we're going to first new object type for our machine um that looks good we can select our machines data set um so our Kafka machines monitoring project and machines I'm going to select that as a title I'm going to give the the machine name and primary key is machine ID so primary key should be unique um but doesn't necessarily need to be human readable and title key needs to be human readable um and doesn't necessarily need to be unique that looks good to me cool we have a machine um and it's already pulled in our properties like install that and sector for us which is perfect next going to add a new object type uh this one is going to be our machine metric and here again we can go to our recent files find our project I think it might be the raw file even though it probably shouldn't be any more anyways but uh machine metrics clean that's the one that we just created with our Pipeline and so these will be just streaming directly in thematology which is going to be pretty cool and we created a title for it and as a primary key we always forgot to put a primary key on in fact our title will be unique um but we could could have done is going back into the pipeline and concatenated the Machine ID and timestamp to have a more kind of stable title um but that's going to be fine for us for now so we created our machine metrics objects everyone we can also change the icon to be something like a river yeah that's uh that's good enough um I'll create a start object for our maintenance requests maintenance request and these are going to be backed by our maintenance request data set so I'm going to go back into our project let me just perfect um and maybe this is going to look like a wrench perfect and again title will be the title and primary keys can be our maintenance request ID gonna create that all right so now we've got those three objects we're going to link these together so our machines should link to our metrics and our maintenance requests so I can go into one of these like metrics and then create a new link type over to machine and we're going to say it links on the machine ID so the metric should link to the Machine by machine ID and that looks all correct to me and now we're also going to do the same for the maintenance request so I'm going to go into maintenance requests I'm going to go down to the sling tab section and create a new link type and we're going to link the uh maintenance request to the Machine by the machine ID that all looks correct so there can be many requests on one machine and I'm going to hit save that I think let's just review all our changes yeah that looks good um I think this all looks great I'm going to hit save and hit save again and as easy to that we have three ontology objects created that we can now use Across The Foundry platform so in the next video we're going to look at building these into a workshop where we can analyze all our performance across our different machines um see some charts have like kind of a drawdown view in our Factory Workshop welcome back to this developer desk side where we're building a monitoring solution for a number of iot devices at a factory streaming data in in the previous videos that we set up our ontology so we've got our machines we've got our metrics and we've also got our maintenance requests and in this video we're going to look at building a workshop so we can interrogate our machines see how they're doing and then raise maintenance requests as necessary so let's start by building our workshop and then we might come back to ontology manager to manage our actions so I'm going to go over to Workshop I'm going to create a new module I'm going to save it in our project that we had um so in our recent files I think it should be there our Capital machines monitoring project and I'm going to call this I don't know uh machine monitor so this is Workshop it's a low code application Builder and that's what we're going to be building are kind of metrics uh module in this is going to be useful so rather than having to walk around the whole Factory looking at all the machines they can just go in here and go through the machines and we can see over time how maybe we might improve this as well so first what I might imagine is planning planning out a layout so I'm thinking having all of my machines on the left in this kind of section here and then being able to see the metrics over here on the right hand side so let's give those names so we'll have this is the machines and maybe these are going to be the metrics and first we'll have an object list so there's going to be listing out or different different uh objects that we could have so effectively our machines required to have a object set so let's create a new object set and we're going to call this um machines and we should start with our machine object and this is going to show us all of our machines um I'm going to sort them by uh when they're installed just because then that'll sort them kind of in increasing order but you can add like a saw key or something like that to them and we can just show the title if we just want to show other things like maybe the uh sector or something like that so we can quickly see which machines are in which sector we could do that easily um but I think this is fine for now and then we'll have a look at the metrics on the right to see kind of uh when we click on a machine that will update this active object so maybe I'm going to update this to our active machine so this is the one we've all selected machine let's do selected machine looks good so when you select something it's going to update that selected machine variable and then we can use it over here on this side so on this side we'll have we'll have a metric card first um and so this is actually going to show us the machine that we've selected some metrics about that machine or just some properties um so we can have a look a creation new variable based off an object property on the machine so the selection machine Let's Get say um the name for example and we've got the name so we'll have machine name um and then also add another metric for the sector so we'll call this one sector and I'll create a new variable from again that object property where we have the selecting machine and we have the sector that looks good um actually we can call this uh selected so I mean sector it's a good idea to rename these before uh it gets too chaotic you can really enable all the things in Workshop um selected name including both like the variables and the widgets so we can rename these metric cards I'm going to keep the symmetric card or we can rename it um we're seeing properties it's going to be and you can see actually on the side when you go to this layout view you can then spot everything and it's easier to understand if you give these names so this is going to be our machine list okay great so you can see the machine then sector if we click on a different machine you can see it updates um that's all working kind of as we expect next we'll actually want to show the metrics in terms of the temperature and production rate and those kind of things especially that I think a graph is probably the right way so what I'm going to do is add a graph a XY chart and we'll plot it as a line chart like sensible so we need to find the objects that we're going to plot here and these are going to be the metrics related to the machine that we are using so I'm going to create a new object set we're going to start from the selected machine right and then we want to find all the linked metrics and I think that should work so this should be um a selected machine metrics that looks good um so you have a thousand metrics for this one that's fine uh and then the property we want to actually plot on the x-axis is going to be the time and Steve wanted to bucket there by year but uh we haven't had the mattress going on very long so maybe we can just do something like bucket it by minutes and then that's probably reasonable um that's good it's showing us account um which is not really what we're looking for um what we're looking for is maybe the average uh temperature for each minute or something like that it's going to do the average uh temperature there we go and we can see that looks about right it's kind of hovering around around 40 it's going up and down a bit but I think that's fine so we set up our line for temperature um that looks good we can see kind of the average temperature over time um what we might also want to do is overlay the production rate so we can say use multiple series and we'll add a series for the average production rate per minute uh production rate on there that looks good um I don't know it's arguable whether you want them on the team axes or you want different axes uh if you wanted them on different axes you could go back and add a separate layer um for now for Simplicity I'll keep them on the same axes uh but I think that's that's all right and so now we have these two series on here that looks good um initially I mean you can hover over and see what which one is which um but I think I might also add in a little Legend so we can see uh how that looks um an inside chart I think is fine uh just you can also see that the temperatures kind of Jump Around quite a lot and these axes aren't very stable so I might first fix the axes and then we'll go into what do we do if we then can like we can go through our machines uh so rather than having to walk around the whole Factory we can just kind of jump through these uh but what can we do next is the action we actually take and we'll talk about actions as fast as customize the y-axis um we can give it a title or we don't really need a title on it uh because we've already got this kind of Legends just remote units um what I'm going to do though is say the minimum maximum are going to be hard bound to zero and a hundred I don't like we can't shouldn't be going low freezing in terms of temperature or below zero in our pressure rate and we're not expecting to go over 100 either so you can see this one uh it looks around room temperatures it's probably just uh off or something and now when we jump between them the axes don't like change in scale which makes uh comparing them more easily uh and yeah 23 is all we can see oh that's running a bit hot and again maybe we want someone to send someone out to to inspect that one um so what can we do uh to to send someone out uh well what we need to do is create an action and then we can write back to our maintenance requests so I'm going to go over to the ontology manager so now I'm over in ontology manager this is where we can manage and add actions first we actually need to add right back to the maintenance request data set uh because then we need to be able to be writing back somewhere uh to store these actions so I'm going to go over to objects and find our maintenance request there it is and on data sources I'm going to add a right back to data set so I'm going to generate this right back to set I'm just going to store it back in our project again and you can see the kind of convention for right back data sets is that the same name as the data set plus underscore edited so I have maintenance request edited and that looks good to me um so we've got this right back to saturated I'm going to hit save um to just kind of create that right back data set and it'll take a minute but that should be created soon so I can then go back over to the object overview and start creating some action types that then start creating these maintenance requests so I'm going to create a new action type for create uh maintenance request maintenance request and we're going to create we're going to add a new maintenance request object um and initially we need to provide a maintenance request ID which is just going to be a brand new unique identifier there's a few other things we could add so we could add the current time as uh when it's created and we can also add the current user as who created it um there's a few other things we can do here um these we can take in as inputs to our action so we can ask the user to select a machine ID ask the user to pass in a request comment um and a title and then the status we're also going to set to a constant value so we're going to say the status is now it's going to be open or something and then when we resolve it we're going to set it to uh closed something like that and that looks good as an initial kind of thing to build um so I'm going to hit create and we're going to check the rules so those are all fine but our form we've got a few unused things here so I'm going to delete the unused properties that we don't need and then drag these to how we should have it so we probably want a title and a comment and then the machine ID maybe the machine ID should be first um I'm not sure I mean what's great about the ontologies you could just keep trying it out and show some different users different ways of doing it and see how that works and in fact rather than having to type in the machine ID so we could do something like this right where you type in you know it's machine 23 is overheating or something we could do is have a drop down of all the machines which can be kind of more intuitive and prevent people making mistakes so we're going to actually uh sort this out to um an object reference so we want an object of type machine so I have to select a machine and you can see here now you can select a machine and you've got kind of a drop down which is exactly what we want um and the title that looks uh perfectly fine um we want it to be quite required that all looks good and the request comment as well uh it looks good but one of the issues is if you're typing lots of things in this comment there's not much space so we can actually go to display and change this to a text area and then we get a bit more kind of space to put it in and the user can also drag and put more context in if they need to add more we can also add a description uh so explain the issue you are seeing with the machine um and then you get a little kind of uh question mark which can help users understand what the things are I'm just gonna change that name to machine just because it is now a machine drop down and that all looks good it looks like we have a working form um I think submit is fine so we don't need to customize that we've also we could set up an action log so every time someone creates a maintenance ticket we can then create some kind of audit Trail um I think we're okay for now but we could add that later and additionally we can set some validation um some like kind of security criteria on which you can submit this for now I'm just going to allow myself to submit uh requests but you could change that in future so different users could uh you know have different privileges maybe there's a group of you know people who should be accessing this and should be submitting maintenance tickets um they can be checked for here so that looks good I'm gonna hit save on that um I think that's all good uh actually it's giving me a warning saying the API name is generated given that we're going to be wanting to call some of these actions from the API I am going to give this a nice API name where I can change it here so this might be uh create maintenance request that looks good to me I'm going to hit save um on that now no warnings great so now we've got our action we can go back to our workshop and then use the section let's go back to our Workshop uh and maybe in this header here we'll add our button uh so we're going to okay plus and add a button group and this button is going to uh you know raise a maintenance request issue so I'm giving a little nice icon um and say when we click this button we want to create a maintenance request that looks good um we can actually set up some of the defaults so by default you probably want to report an issue with this machine so you can say the machine to use should be the selected machine that's good um and what we could do so here it's going to pre-fill with machine zero if we wanted to we could make this disabled so they can see it's pre-filled with machine zero and they can't change that which is useful for preventing errors again or you could hide it completely but is up to you that looks good I don't think we need to do anything else there um that's all good so now we can afford an issue and say uh well what was our example our example was I think uh machine number nine uh we were looking through the machines machine number nine isn't producing anything uh so I'm gonna report uh not producing anything uh machine nines uh production rate has been zero for the last couple of hours um please can you look into this great I mean submit that off um and that creates a maintenance request so we haven't like got a particular view to see these major requests so maybe we can do that now in Workshop so at the top here we can have a tab for viewing our machines and we can also have a tab for viewing the maintenance requests that we've submitted so I create a uh tabs that's good so our first tab will be our machines uh and then our next tab will be uh maintenance requests so I have to do now is set up some layouts so we're going to switch between these when we change the tabs so we have uh one kind of default page layout uh if we want a new page as well and this one we might call our maintenance requests page and maybe we'll call the other one um machines page right something like that um that looks good and then we can hook this up so our machines when we click it we should switch the machines tab um and then our instance requests tab when we click it we're going to switch the maintenance request thing so now we can switch between these look at that and we can have these two different parts of our application so now our next thing to do is actually fill out this message request thing I think we'll have a similar thing where we have our message requests on the left and then we can see our maintenance request on the right um so I'll put requests and then maybe this is one you know particular request uh we can have a object list here um where we have a list of all of our Matrix quests and create new object set which is going to start where they are or requests uh now we can see our request that we just put in not producing anything maybe in this case we might want to see a few more things like when was it created that and possibly the status as well so you can see this is still open still needs to be investigated we could add maybe an assignee as well if you're making real ticket workflow um you can see the other video uh Logan did on like a more kind of kanban style board we have maybe more flexibility on this again we're not going super deep into that you can see that video for that uh but this is you know good enough for our purposes so we can see the request and then over here we might want to show The Selective request uh so we have this object list active object we can call this uh selected um request and then here we could show maybe the object view I'm going to keep it simple of the uh selected request and now we can see the kind of various properties on the request so other things you might want to do on our request you might want to filter you know who's created it when was it open the status Etc what we could add is a very simple filterless component so I'm going to click above to create a new widget above and pop in a filter list for this we're going to filter all the maintenance requests down so we've got selecting all mentions requests and maybe we have a filter to search by uh let's come and just do the status for now you can also set this to display at Auto height so then we can click between these currently they're all open so the status is just open whatever multiple values it would show the you all of them and you can finish down through them so uh the problem is though is this list doesn't actually filter this object because we're using the same object set so what we actually want to do is have our filtered maintenance requests shown here so we'll create a new object set for our filtered requests we can start with all of our requests and then filter using this output from this filter from this filter kind of list here so now when we would select our value we could uh see it so if we swap this to exclude Open tickets we then show nothing which is kind of exactly what we're expecting um great so I think that's uh good for our maintenance request dashboard now well the next problem is we can't close the request so we want to be able to resolve a request so I can go back to ontology manager add that action and use that let's go back to ontology manager again you can kind of do this very iteratively where um you go through uh and build your Workshop out and as you're building it you can build those actions out so back of our maintenance request object in the ontology manager here we can see our make create maintenance request uh action type but now we want a new one so we're going to create a new one and call it um resolve instance request I'm Gonna Change object we want to select a maintenance request to modify and then we're going to change the resolution comment to add that and we're also going to change the status I think um and the status we're going to just change back to closed and the resolution comment resolution comment I think that's all fine um let's create that object um that looks good I'm going to get rid of the unused parameter again um and as before we can go into this parameter and change it to be a text area so that users have a bit more space to kind of type in their comments there I think that's all good again only I'm allowed to submit it and we'll probably have to come back to change that later but I think that's fine we're also going to keep an API name so we'll call this resolve maintenance request um and hit save and Save confidence update hopefully we can come and use it so actually I'm just going to save this and give it a Reload um and then we should be able to use our new action so we've got our object here again we'll probably put it in the same place just because I usually expect things to be in the same place so we create a new button here and we'll call this uh resolve request and this is going to be like a issue tick I think that's an issue issue tick yeah that looks good um and so you can see the kind of flow that maybe some operators are reporting issues and then maintenance teams go out on the ground and they can open this up and resolve the tickets off uh from there so they can resolve it and when they click it uh it's gonna call the uh resolving this request action again we can pre-fill in the maintenance request object with our uh selected maintenance request and so that means like when you've got this this maintenance request open it'll automatically select that there um and again for consistency we'll set it to disabled so when you click it uh you can't then change that range of request you've popping a resolution comment and then it's closed saddle looks good uh we can test it straight away so I'm going to try resolving it and say we looked at this um and you just set it on you need to turn it on uh lcms to be working correctly something like that and hit submit and you can see it takes a minute uh to think about that but the status is now updated to closed um and if we look at the properties we've got both the request and the resolution comment great so there we have it there's our simple Workshop where you can see our machines right we can go through all our different machines see how they're doing in terms of metrics and then raise issues when we want to report it to the ground maintenance crew once they go and fix the machines they can then resolve the tickets and explain what the problem was a very simplified workflow in reality you might want to extend it maybe your maintenance tickets have more states where they get handled over between different people or they're kind of investigating or in progress Etc or assigning uh different maintenance tickets to different people you also might want a better commenting flow So currently you can put in one comment where you file a ticket and one comment when you resolve it but you could have like a series of comments or maybe have a maintenance request comment object that you could also link to this object there's also the possibility of adding automated alerts so with Foundry rules you can automatically alert when the production rate seems too low or the temperature seems too high or there's some combination of more complex factors where you want to look out for these things or going even further you could use something like found gml to spot with machine learning what issues might be happening when you're producing these things in the next video we'll look at extending this with a custom react app we'll use The Foundry apis so that the maintenance team can actually read and write from a completely separate application right back into Foundry thanks for watching and see you there so in the last videos we set up the ontology here we're actually going to jump straight into coding we're going to build out a basic Express server and then we're going to see how we can call the foundry apis from that server so let's start up here I'm going to make a directory for our server so we can later add our react app into its own folder um what we'll do is install some dependencies first so we'll have node one for refreshing our Server Express for actually acting as a server and axios for making requests out to The Foundry apis so it says get installed we can create uh index.js which is going to be for our server and we can start off by importing Express so we can add Express and then we can create an app like so add an endpoint so for example we could add like a endpoint to get just at the main route of the server and then we're going to have a request a response pair that we want to create a Handler for and for now we're just going to send a really basic response uh saying uh you know hello world great so we've created our hello world endpoint and then we might want to actually listen for connections uh so we can set a callback and then uh log that we are listening uh just so we know where it's going we also probably want to pass in a port so we could say you know Port 3001 for example um it's what we're going to listen on uh listening on Port 3001. and then uh if we run our server we do npm start uh what we need is a script in our package Json to do this so how we're going to do this is adding a script block so inside our package Json we can open it up uh we want to add to Scripts and we have a start script which is going to look like uh node Mon index.js and so this is going to monosh that file for update and run it whenever it changes so hot Run npm start you see it's listening on Port 3001. and we go to 3001 uh we get a message back saying hello world great that's exactly what we're looking for and so actually if we update this thing so you know we'll add some more explanation marks hit save and refresh we get an updated message so that's exactly what we're expecting we've got our basic server up and running so now what we'd really like to do is have our server connect to Foundry and proxy any requests through so if we ask for the server for requests uh or maintenance requests uh it will get them from Foundry and deliver them to us and similarly if we tell it to resolve a request it can then apply the right action on the primary side and the reason we do this through a proxy server you could just hit it directly uh depending on the course configuration specifically on your calendar Foundry instance um but the reason we're going to do this through a proxy is you imagine there might be some extra authentication they need to do or it already integrates into an existing API and we're going to show you how to do that today so what we need to do now is actually connect this to Foundry and the right way to do that is creating an oauth application which you can then as the authentication and our authorization to call the right resources so we'll do that now again all this and more is available in the documentation you can have a look at the documentation the developer documentation tells you how to set up or auth tells you how to call the apis um but I'll show you how to do that now so I'm going to go down to my account and then settings and then third-party applications I'm actually going to make this a bit bigger just to give us some more space and we'll create a new application so register new application uh and we might call this uh maintenance maintenance app um and for now we're just going to say this is the server application so we've got a confidential client that's good we've got a client ID and client secret uh we'll copy those down uh so we remember those so we'll say our client ID is that and our client Secret [Music] is that you can regenerate these uh when you do the previous ones will be invalidated so you want to remember those good so this is our new oauth application and we scroll down we can actually see uh there's some authorization Grant types currently it's defaulting to the authorization code Grant which is when you can act on behalf of your users so if the maintenance team had Foundry accounts uh they could sign in through Foundry effectively and then you could call the apis on their behalf what we're going to do now though actually is set up client credentials Grant um this one is a bit simpler in terms of implementing it and maybe it's useful if you're already doing the authentication on your side but it basically gives you a privileged token specifically for the application so I can stay with the authorizer codes Grant and enable the client credentials one good and so you can see we have a service user added that then has the permissions associated with them that's good so I'm going to hit save on that and also what we're going to have to do is gonna have to enable this application so I can hit actions enable application and for now we're going to just enable it with unrestricted access if you wanted to you could lock that down to a more specific subset of who has access that looks good um there's probably a few things we need to do now so in our ontology manager we're going to have to give permissions for the maintenance app to call those actions so in our actions when we want to resolve maintenance request we want the application to do that so we can go to the security and submission criteria currently only I can submit the action we want to change that so rather than looking at the user we can change that to from me to that new Services we've created called maintenance app that looks good if you wanted to you can change this to a group or you could add some logical operators or you could have hands and ORS to like configure this more carefully but I think that's all good for now also going to hit save that should be good we should now be able to call this from the API so how about we then do that so again I'm going to look at the documentation myself actually um and so I can go over to the documentation scroll down to Ru developer and then view the API Docs that's what we're going to see and then a few different endpoints most relevant it's probably going to be listing uh the objects that's what we want to do first so we want to list all the Matrix requests so we can display them in our application and then we want to apply the action to to resolve them once they are resolved and so this page is the documentation for the standpoint you can see how to call it you make a get request to this thing explains all the different parameters are you can pass in as well as the response that you can expect to get back so going back to our application um what we need to do is create a new endpoint where we can get the requests from and then under the hood what that does is it runs the client credentials Grant flow to get a token for Foundry and then calls Foundry to list the objects so let's jump into that first off I'm going to create a new endpoint so when they get the request endpoint we're going to respond with the requests so we have a question response and so there are two things first uh use the client credentials to get an access token or Foundry and then secondly we're going to list the maintenance request request objects and third we're going to return these to the client so how do we actually Implement these steps well we're going to have to make requests out to Foundry so we're going to have to import that axios Library we installed earlier second it's all require axios and again you use your own request making Library here I'm just using axios as an example I'm also going to paste in some sample code which is going to hit the right endpoints so here we have uh axial start request we're sending a post request to our stack which is devulations eternal or pound to foundry.com multi-pass API or2 token so this is the or tool token endpoint and we're going to be passing in some parameters alongside that as well so we want a grant type of client credentials as we said we're going to use the client credentials flow to get an access token and we're going to pass in our client ID and client secret remember we wrote These down earlier when we created our application so I'm just going to pop those back at the top um and store them here so our client ID and so the client ID itself isn't secret and it's fine to kind of uh store around the place uh the client secret should be kept secret so you wouldn't want to hard code it in like this what this should be done is uh this should be put in some uh kind of Secrets management solution like Eris parameter store or hashicorp vault or something like that and got an out of that at runtime securely but for now I'm going to leave it there um the other thing we'll notice is in this code snippet we use this away keyword which is for doing async things like Network requests in JavaScript um to fix the arrow here I'm going to put an async in front of this body so all this runs asynchronously great so we've got our access token that we can now use to make requests to Foundry how do we actually make the request good question so in this case um we're going to hit this list objects API um so we can probably do that ourselves pretty easily so I'm going to actually just copy this same request and this is going to be our objects response in this case it's a get request to the same base URL but to the API if you want copy this in um let's get the ontologies uh and you're interested and I've told you read parameter and the object type parameter and you can actually see the explanations for what these parameters are here um so we need to fill those in so I'm going to use a template string to put those in um and ontology rate we'll have to Define as so as object type so ontology read and object type and the way we can find these is if we go back to Foundry inside our ontology we can go to Advanced and down here we get the ontology grid so this is the resource identifier so I've copied that onto jirid you can see it's ri.ontology.main.ontology so this like final ontology tells you it is a ontology red and then it's uh the ID there and then the object type ID if we go to the object so this was a request so really this is not really the object type ID this is the API name so at the Matrix request is getting a bit uh tight here but it's this API name here so I can uh I can just copy that there um and pop that in because that should be everything we need we're now able to make objects uh requests and we also want to get out the objects from that response so you can do uh obvious response um dot data dot I think data again because it returns a object with a key data according to the API right uh back here the response probably has data which is a list of our ontology objects we actually want to return that uh so that's what we're going to return to the client um by doing res dot send and sending that data back to the client you might want to do some more validation I would assume you'd have some kind of uh like Authentication uh first um and then maybe you know some more validation here that this is the right kind of data that you should have access to Etc so if we go back to our server and we try slash requests it it did not work because we were missing credentials right because we actually got our access token but then didn't use it so inside our objects response we're going to want to provide um so we can govern our parameters we need to provide our headers right uh so our headers we're going to have as authorization or authorization um and the value should be Bearer and then the access token we got so we refresh now it works look at that and now you can actually see our object here um so it's a bit hard to see because it's just Jason I can zoom in a bit but you can see this is our request ID this is the request comment we can see the thing we wrote earlier between nines production rate has been zero for the last couple of hours please can you look into that so if you watched the last video when we move on the workshop you can see that is the ticket we wrote then great so now we have these objects we're able to get them from The Foundry API we can return them from our um our own server API um what we want to do is then be able to resolve them from the API and so that for that we need to actually apply the action we created earlier so let's jump into that um going back to the API reference uh what we now want to do is a apply an action and so we can actually probably copy and paste a lot of this um but I'm going to first uh extract this uh getting authentication token into its own function so we can tidy up the code of it so we can have our access token is uh wait access token um that's good I'll create a whole function up here I can access token uh it's going to be an async function uh that just runs this thing and returns for us and that's going to help us reduce the code duplication a little um otherwise we can have maybe say I'm going to keep this as a get request for now just so we can kind of test it out and then we'll change this maybe to a post request which might be more so I'll call this resolve request um and then we've got the access token and then we have the same ontology grid and action object type actually we don't want this to be an object type we want this PN action type uh be this URL as well so you have the action type and the ontology red and this is a post request we can see over here so we're going to post which we want the same access token set up an action type action type wasn't maintenance request so it was resolvements request so we can get just the same API thing here um which is called resolve maintenance request I think we set that in a previous video as well um that's good and this one we actually need to send the uh request for how we're going to apply it so for example what are the values in the form um so for that we can send in a good body uh or data I think it's called Data um I'm going to send in some data and the data should be a map of parameters um so our parameters should look like a map of primer hoodie to parameter value so uh the parameter ID um might be if we go over to the form I'm going to make this a slightly bigger um the maintenance request is going to be a parameter we can go to if you go to details you can see the ID here um and so the initial request should be some kind of ID um I think I'm going to copy uh this main search request ID for now and then in future we can pull that out in the uh like into our actual parameter resolution comment as well there's the ID we can copy that there um like so and the resolution comment we can update to uh I was updated in the API great um that looks good uh and so we can send the response back uh from when we make the change we can maybe see down here what the response should look like um so it's just uh empty object not so interesting so I think we'll just say uh message uh success return um so now if we try and run this where we do resolve request we can see we've got back a success response and if we refresh our requests API we can see the resolution comment was I was related in the API and in fact if you go all the way back to Foundry in our Workshop that we built previously we can go over to the machine monitoring uh application and we can see our request and here um we can see our resolution comment I was objected in the API so everything is all updated um from making that one API request great and take a little break here and then next we'll build a react application that's going to hit our new server and be able to manage these requests so before I took a little break we've got our server set up um there's probably a few things I want to tidy up about a server and then we can start using these endpoints in our react app so uh easy thing is just remove this unused variable the other thing is that we've hard-coded this uh request ID in here um so what we want to do is actually take this in as a parameter so we can do uh request dot uh query and then the request ID and that way we can pass in a request ID when making uh calls to 10 points so we're taking in as part of the request um so I think if we start up the server now uh we're listening so just check this one works and refresh up yeah that works uh we can also do resolve request and now we need to pass in a request ID and if we pass in the same request ID that we had earlier we can just check that works that's success so for example if we added some more exploration marks to hours updated in the API I tried that again um and we updated the request when we refresh here we can see at the exclamation marks are added so that is all working as we expect so I can create a new project uh new react app with create react app so let's uh try doing that I open a new terminal for this one um and we can do MPX create react app um and call it web and we can actually use the typescript template uh just to give us a bit more type safety I appreciate it it's a bit of a weird mix that we've used just plain JavaScript on the back end and typescript on the front end but but hey I get to get a bit more variety and a bit more uh excitement obviously you could use whatever language you wanted uh both on the front and back um but this is what we're going with so I'll take a few minutes to install uh when that's all done I'll be back with you okay here we go uh it's now completed running uh the script it's set up our web folder and now we have a react happen here um I mean very quickly we can go inside of it and run npm start and that will automatically open up in our browser and we get to see uh our little website so I'm gonna open up app TSX so we can start to make changes and we should be able to see them up here on the left hand side make this a little bit smaller um give us more room for coding excuse me edit SLC app not GSX and say true load so you can say I'll do this to hello world um or you can see the update pretty much uh instantly so we're going to use the access Library the same one we used on the back end uh to call Family apis we're going to use on the front end called our API so let's try installing that I'm going to stop the application for a second and install axios and give that in one minute um once it's ready we know what it will look like we can import axios from axios um and their application we're going to use react hooks to store whether we've made the call or not you might want to use a state like Redux or stand or something like that um if you're really doing this It's Gonna Run start again just so we can kind of see our changes live and so we can make sure we're not breaking straight away so uh sources hello world that's good now let's try and make a request to get our requests that's a bit tricky uh we'll say there's going to be our maintenance requests we're going to use state I'm going to start with just a empty array for now and then we'll use an effect we're just going to be run once uh when we call a function what this is going to do is it's going to use axios and we're going to make a request um and the config is going to be r which is at localhost 3001.

um our URL or slash requests and the method was get right and so that's going to be simulating as we ran uh requests here and it should be able to get this list of things um so then after we've made that request we're going to have some response um and the response data should be that array and so it will store maintenance requests uh requests that makes sense quests because it's a hook um and we'll set the date of that for now I'll just uh like put them out as a string I think we might have some issues but uh we'll see a refresh okay we have energy array um I suspect there might be a cause issue here but let's see if I'm correct access error unsupported protocol host ah we just forgot to put uh that should be using http um now we're getting a network error so now if you look at Network yes it has a cause failure so the issue here is we're hitting cross origin resource sharing issues and this is because we don't want to be able to hit random apis from our website we want to kind of limit which ones we actually hit um and also have the apis limit where they can be hit from uh so it's harder for an attacker to take advantage of a cookie or something like that placed on a user's browser and maliciously use it against the user in this case we're not using Google authentication we might be but you know uh in this case we're going to say we're not um and we can just uh quickly install a course package which will allow us to get around this restriction um so we're going to go into our server install the cause package um and then put that here so we can say cause is required cause um and then we can use it in our app so app.use of course there we go if I hit save Let me refresh here we can see in our react application we've now got our objects right we've got this kind of Json uh list of things which looks right um and so we've got maintenance requests and now let's display them in a table so here I'm going to remove this image I'm going to strip a lot of this out it's really going to look uh look kind of ugly but it should uh should should suit our purposes so we'll call this uh the mention this app um and let's say we'll just have a a list so we'll have um our order list uh and where the maintenance requests I'm going to map each request um into a list item so list items going to have the request um let's get the properties and then the third title um I think we had a title right yeah a title um which is good um it doesn't know what the request is yet so we're gonna have to define a type for it so we'll say this is a main 10 tenants request and array of those um and we can Define the type here so we can say it's in Facebook request which has a red string and then properties and then properties look like this so we have minutes request ID the string this comment the string resolution comment is it showing oops let's just call it as a string that is also a string machine ID machine ID string title this string and then created by this also a string and then status as you there um opened open closed that looks good to me um so now we've got our request here we can actually uh get rid of this one um we can say oh it's not producing anything um was our request and so you can see our request in our react app we've done uh displaying them maybe we want to tidy this up a bit uh fix the CSS I'm not gonna make it uh look beautiful here but um I'm gonna remove some of the things that we don't need I don't think we need any of that um and we'll also we kind of removed everything here um yeah that's right let's see how I mentioned that um and yeah what's that massive uh margin of that thumbs up and we have not reducing anything and then maybe underneath uh we'll have um the request comment so the rest properties and then the request comment uh and we also will have the status as well in Brackets or something so we have uh requests properties um status something like that or we can see something like that yeah that's good again you could spend a lot of more time uh making this look like nice um but I think this is okay for our purposes um great I'm gonna pause there I'm gonna uh start again in a second take a little break and we'll have uh some buttons that we can then click and that will call back to The Foundry apis to update our requests see in the next one so I'm back with a new camera battery and we're good to continue so we've got this uh list item element uh here where we're displaying the request all you'd like to do is add maybe like a input and a button so that they can type back uh the their response and then hit submit to kind of close that off um so first what I'm going to do is I'm going to extract this out to its own component so we're going to call this I don't know uh request view something like that and we're going to pass in the request um and we can actually create that now request for you um it's going to be function component where it takes in a request and we can find the type of that as having a minister request property um looks something like that um and then we can just return the same thing we had before maybe in a paragraph talk and let's hit save and we can see uh that is working as we're expecting just checking that it's working we can add some exclamation marks great looks like it does um salad is all happy now what we'll probably want to do is add a input here so we can add input and a button and the button is going to say uh Mark has resolved we can wrap this all in a fragment uh to that that looks good um so now we can type something back and mark that to get as resolved maybe we want to hide this if the status is already closed for now I'm just going to leave this like this and we can uh we can change around um if we wanted to and what we need to do is control this input or at least be able to get some of the value out of this input so we can either pass in a reference and then pull the value out or we can store the kind of value in state and I'm going to go for the second you could also maybe use something like react hook form or you know a formic or a library like that to help you manage your form state so let's do resolution we're going to call resolution comment and set resolution set resolution comment um is going to be you state and that we're expecting it to be a string um so that's fine uh and then the input value is going to be that and then when we change the value in our input we want to then update the value in our state uh so we're going to have the um event um and then we can set the resolution comment to whatever the event Target value which is going to be the value uh in the field so the event Target is the input and the value is going to be the value that it's just related to um so we can see we can now like still type things in um I don't know be stored in this resolution comment field for us and then we click the uh Mark as resolve button we want to hit our API so I'll just say on click um we can call update request and I'm going to find that function here so update request that's not going to take any arguments but it's going to send us uh send us updated request let's go back to our server API and we can see what we did there so we had um this resolve request we probably uh posts that's probably more accurate um resolve request endpoint where we were going to pass in the request ID as a query parameter and we also need to actually pass in the resolution comment as well I've realized um so let's do query um resolution comment uh that's good we could also pass it in as like a body but I think that is uh is fine um so here uh what we want to do is make a request um and again we can probably copy uh this stuff here oops this stuff here but in this case it's going to be a post request and we're going to do to the URL resolve request and as parameters oops oops uh parameters uh we want to add that the request ID is going to be the request uh please uh in this request ID and then the um resolution comment is going to be our resolution comment that we've just been typing into this text box so now let's give that a test so we've got our field here we can type things in and we can click Mark as resolved and hopefully that should have sent a API request to Foundry so I actually would send it again and we can see we've sent this resolve request and we're getting a 200 back which suggests it's happy and so if we go back to our Workshop um just as an example of where you might see the object and check that's been updated we can see this property should have been updated yet so as K dlfj uh whatever I typed in here as you can see we're writing back we could also actually show the resolution comment in this as well um so maybe we can do request properties resolution comment the only issue is this won't update uh immediately uh because we'll actually want to be updating the requests uh in some way we could want to like rerun this uh use effect code that's basically the end of it appreciate this is very uh simple application we can do a bit of testing if we want and just try things out so for example if I went back to my machines Workshop um and then I said you know I want to report an issue on routine number five um say uh please look into this and then left some kind of uh helpful comment right and then submit to that um we'd see that was applied here and if we refresh our react application and again you can change this to be Auto refreshing we can see that uh application that comment comes in through here we can see uh we've said something here and we can also Mark it just all back in here so we can say uh looked into it uh into it great work is resolved um and I can actually manually refresh this and you can see that it's pretty much closed and I've added comment looked into it um but then also if I go over to my main sense requests and see where please look into it um I can see that it's been closed with the resolution comment I've looked into it so it looks like it all works right uh all those updates seem to be passing through and I'm pretty happy with that I think that concludes our Foundry developer desk side for today just as a recap of what we did is we set up a Kafka cluster and then we started streaming the data into Foundry we then transformed that data using pipeline Builder we then construct an ontology of machines uh maintenance requests and machine metrics and we then built a low code application in Workshop using those where we could go in monitor the machines and see how they're doing we could then use actions to raise maintenance requests as well as built a tab and workshop where we could manage those maintenance requests and then outside of Foundry we use The Foundry apis to build a fully custom react application where we can manage the same data in a different application thanks for joining me today uh we'd love your feedback on whether you enjoyed this long form content whether you enjoyed this kind of custom development seeing how the apis integrate uh and also you know we'd love to hear what you're building on Foundry so please do comment below and we'll see you around [Music] thank you [Music]