# Code Repositories | How to Use Data Expectations in Palantir Foundry

[Music] it's a really bad idea to allow bad data to propagate across your pipeline to prevent this we have data expectations that allow you to assert that your inputs are within the parameters that you expect and they also allow you to assert your output is correct if your inputs are incorrect your build won't even run the data will get stale but it's better for it to get stale than to have bad data that will then propagate to another team or another group doing machine learning on top of your data using it for training which then can generate a classifier that's going to be used in another group trying to do marketing or fraud detection so let's assert our data both at the input and at the output so that we can make sure that we know what's being propagated and avoid corrupting the pipeline the longer bad data goes undetected the worse it is so doing this as a day-to-day practice is a great idea let me show you how it gets done we're again in the foundry reference project this time we're going to add data expectations to the ui layer or at least the pipeline that packs it which is deontology project aviation let's start with flights opening flights we see that there's already that expectations passing let's go and check what that expectations have already been added we can see that there is one that expectation checking if the primary key is unique we can see the primary key is generated opening the description shows us that the generation is based at average columns let's test something more meaningful for example departure versus arrival of a flat here you can see that you have both columns for departure and arrival the expectation would be that the arrival always happens after the departure so if this doesn't happen it's a data problem let's add a data expectation for it we start by creating a new branch because again master is locked and good practices tell us that we should not push code directly into master so the new branch we call add.expectations here we already have one check with all the imports then already so let's just add another one we can say check for the column departure and we can then search through the autocomplete for the type of check that you want to add in this case we want the is before another column selecting it we can say that departure is before the arrival and we name this check departure should be before arrival we tidy things up and run the check in preview mode preview only snapshots 1000 rows so the check failed from the 1000 rows 391 have failed the check the preview gives us a quick example of where this check has failed for example this arrival is almost one day before the departure this is wrong represents that the problem we're not going to investigate a lot of problems here but it's good to know that they exist we can then go back and fix it running a full build shows us how many total rows actually failed against this check after the build completes the build itself fails because the check has not passed this prevents that bad data from propagating and it gives us a quick example of where the check passed it also tells us that from the 4 million rows this dataset has almost 200 000 failed to check this again does represent the data problem it's up to the developer to say if they want the bill to fail or for it to just get a warning in this case we're failing you can see the failure boss in data health and in the reasons why the build failed itself going back to the code let's add another check this time let's check for the origin airport and instead of checking at the data set we're working in let's check the input which also has the same column airport just for an example we're gonna check if the airport code is three characters at the input we add a new check where we verify the column origin which is airport origin has exactly size of 3 characters we also named this origin airport code should have 3 characters and instead of failing the build we're just gonna ask it to hardness if it's wrong running the build actually shows us that our test has failed and the reason why it has failed is because size doesn't work against strings it only works against arrays or maps but i still want to test if the airport code has three characters so let me go through the autocomplete and see if there's any other type of assertion that i could use instead of the size which is wizard for horizon maps so going through a quick scroll i can see that there is actually something that looks like a rejects so our like is a regex let's use that because then you can just verify if it does have three characters we add a reshacks in place for checking if it has three consecutive characters we hit preview and our that expectation will now pass with this we have now added that expectations at both input and output for this data set thank you for listening and goodbye [Music] you