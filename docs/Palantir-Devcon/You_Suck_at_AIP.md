# You Suck at AIP

if you're still using AIP the same way you were 6 months ago you may not be using it to the fullest let's look at an example to see what's [Music] new for this example I'm going to use blueprint which is our open- source uh UI toolkit so uh this has a bunch of different components in here things like uh different buttons and forms uh different controls that might be useful in building an application and one of the things that it has is uh a bunch of issues on GitHub so this is where people can report bugs or ask questions things like that um and one of the challenges with a popular open source project is that you end up with many many issues and some of these can be duplicates of issues that already exist so we're going to see how AIP can help us duplicate some of these issues by building something that can automatically suggest duplicates so let's go and take a look at an example issue that we might have here so uh there's this issue uh that I found that mentions that the file input has no visual Focus State um and this is one that has some details on how to reproduce but also is closed because it was fixed in a uh future version so if someone new comes in and searches for uh an issue because let's say they're on an older version and uh they're seeing the same issue um they might not be able to find it so for example if I go in and search for file input Focus outline I'll see some other stuff in here but this issue doesn't come up and this is because GitHub search here is doing keyword search uh and so unless I figure out the exact right keywords here it's not going to find the issue that might be relevant for me so let's see if we can use a AIP to do that a bit better in this case uh I've brought in uh these GitHub uh issues into a simple pipeline that goes in and extracts out and converts this into an object so I have a blueprint GitHub issue object and I also have a linked set of uh issues uh issue chunks so these are pieces of that GitHub issue that I've broken down into smaller segments that I've then used an embedding model to uh to create a an embedding for so we'll come back to that and see how that might be relevant in uh our search later on but I've created some of these these issues and they look something like this so uh I have a series of issues uh and they have things like the state or uh the body some different URLs for uh the different parts of whether it's starred or not things like the assign uh what type type of user created it some comments bunch of different fields um so what I want to do is I want to use AIP logic to build an issue D duplicator so to start I'm going to take in a parameter of just uh let's say new issue right so as someone creates a new issue maybe they have some text I want to be able to go in and say um let's check if that text is already a duplicate of an existing issue so the way that you might be doing this is you might go ahead and grab that use llm block uh which lets us start using a language model to uh complete this task you might put in some instructions something like hey your goal is to identify duplicate GitHub issues the user will provide a GitHub issue use one of these tools so in this case maybe the query objects tool with that GitHub issues object so in this case I'm going to bring up that GI up issue uh and I'm going to pass in that new issue context and so let's go ahead and run this with some example issues so uh I'm going to go ahead and run this with that same example that we had before so file input uh Focus outline so let's see if that works so I'll go ahead and run that and we can see that it actually goes in and is able to define a query for uh to use and we get back some results so in this case it looks like I found some other issues around file upload um something about Focus States but it's not the issue that we wanted it to find it didn't really find that uh issue around no uh visual Focus state so let's see how we can improve this initial version using some of the new capabilities in AIP so I'm going to save this on our main branch and I'm going to take advantage of our first feature here and I'm going to create a new Branch for our V2 vers version of this um I'm also going to go ahead and add this as a test case so that I can run this a couple more times and this will create an evaluation Suite forming so I'm going to run this on our initial version just to see how well we perform out of the gate and then we'll come back to that when we when we make some changes so uh after uh this runs I am going to go ahead and switch to the V2 version of this Branch so I'll go ahead and create a V2 version and then start making some changes so the first change that I want to do is I want to uh do some of that work to find the relevant issues out of the gate so we can have the LM use a tool to do this but we can also use some of the new Tools in AIP to do this so I'm going to use the semantic surge transform to be able to query for the relevant issues deterministically so we actually save a little bit of back and forth cuz we know exactly what we want to look up uh and we want to get the set of other GitHub issues that are semantically related to the new issue being created so this uses that embedding that we defined previously so in this case I'm going to go in and Define what object set I want to pull that from so maybe I want to pull this from that same blueprint GitHub issue object set and maybe I want to do some filtering on this first maybe I want to go in and say uh I want to filter this object side maybe I want to start by taking all of those issues and filter to issues where the uh state is open so maybe I only want to filter on uh open issues so I can specify what my filter criteria is and another convenient feature here is I can goad and select these and just run this set of features see is this working in the way that I expect so I'll go ahead and run that and I see oh I'm getting zero blueprint get issues uh in this in this result here so that's a little bit weird uh but now I can take a look at the actual debugger and see the objects underneath to see why this might be so if I open this up or go back to my uh previous list I can say Okay looks like it might be lowercase here and I specified uppercase so I'll switch this to lowercase rerun it and I can quickly iterate and say okay cool now it's narrowed to the open GitHub issues instead of uh showing uh all of the GitHub issues so going from this 7 6,700 down to 742 um and I can do that and quickly test even subsets of this of this workflow in this case I think I actually want to work on all GitHub issues so I'm just going to remove that and search across all G up issues so uh I want to pick the right property search and semantic search works across our embedding property but I'll notice that I actually don't have an embedding property over here um and that's because this is on a linked object so I can go and create a uh linked object set by using our search around capabilities so I'm going to start with all of those uh GitHub issues and I'm going to navigate to the linked chunk issue this CU one GitHub issue might have several chunks associated with it depending on how long the GitHub issue is so in this case I'm going to navigate to those issue chunks and I'm going to start using those instead so instead of using all issues I'm going to switch to those issue chunks and I can rename this as I go maybe it's a little bit clearer to say uh linked GitHub is Chunks here great um now I'm going to go in and search on that embedding column that's available on this object and maybe I want to get the top let's say 10 objects and for the query this is what I'm actually using to search and I can put an embedding in here and do that embedding wor uh work myself but because I've told my ontology exactly how to do this embedding by describing the the model that's used when I configured this object I can just put in text here so in this case I'm going to put in that new issue text uh that we're taking in as a parameter so I can quickly chest these three blocks uh and see if those behave the way that I expect in this case I actually haven't specified a value so let me go uh specify that value that we uh picked before so I'm going to uh pass in that file input Focus outline great run that all right uh looks like that is uh and let me just run these blocks instead of running everything all right looks like that is returning 10 issue chunks and those are the most related issue chunks and I already see some promising signs and this tight iteration Loop is really valuable to me here CU I can quickly see ooh file input has no visual Focus state that is actually that issue that I was looking to or at least a part of that issue that I was looking to retrieve earlier so now I have that issue uh I know that I can start using this as uh a building block for the context that I pass in getting that right context into the model is super important so um I've done a bunch of different steps here and uh as this logic grows in complexity I want to make sure this file is still manageable so I'm going to select these and group them into a block so I'm going to call this uh relevant issues and I can actually go ahead and collapse this so now I can take all of that complexity and kind of group it in here and hide it behind this um I can always go in and expand it and change it but uh this makes it a little bit easier to manage as I try and build more and more advanced functions here so the simplest thing that I can do now is I can go ahead and take this and update my llm block to instead of uh having my llm do the querying I can pass it context that I have quered for deterministically so maybe I will say possible issues here and I will pass in instead of passing in uh asking to query for it I will pass in uh the issues but I see the first issue I'm actually not uh working with issues here I'm working with trunks uh and so before I pass this in what I want to do is I want to go back to that block and do one more search around I want to Pivot back from my discovered uh relevant chunks into the objects that are relevant here so now when I go and reference that I see okay cool working with my object again so I have all the richness of the ontology that I expect and want to use so in this case I'm going to go and pick some subset of the properties here just the ones that are relevant for answering the question so maybe I'll pick title maybe the body of the issue uh the URL so we can put some links back uh and then uh maybe the state to check whether it's open or closed which might you know suggest something different uh in terms of response whether hey here's where you can go figure out uh how to resolve it or here is an open issue that still needs to be fixed um I also can go and configure and say actually there's some extra stuff in here like maybe the object uh rid that I I don't actually need um so I'm going to change this instruction from responding by using the tool to saying uh consider the below issues uh when looking for duplicates uh and then I'm going to say return the duplicate issues in your answer so this could work but I also have a suggestion by AIP here which is switching to that single completion mode so right now we're using this process where we ask for the LM to determine uh whether it should execute some tools or whether it should generate a final answer um I'm going to switch that over to this single completion mode because we don't really need that in this case because we're providing all the context to the model we can just go ahead and ask it to generate an answer directly so once we have this I'm going to go ahead and rerun that and we'll see what we get so this case it's done that first group of finding the relevant issues then it's t taken that and passed it into the model but we see a little bit of a challenge here where it's passing it in a pretty messy way um we have some markdown formatting that we're using for this because structure is really important when giving instructions to these uh to these models so having as much clarity as possible uh helps quite a bit in the quality of output but what I'm seeing here is this is using our default formatter uh and so we're getting this as a CSV and it's pretty difficult to read even me as a human looking through this now it's kind of difficult to say is this part of the instructions or is this part of uh the details that we are passing for this specific issue luckily the model does seem like it's figured out some part of it but this is a pretty messy answer and I can use our markdown preview to see what it looks like rendered um but it's still pretty messy um and so uh we have started to find the right thing which is it's returning the right issue as the the first result but I think we can do a bit of a better job uh to see if we can provide the model with an easier format to get a better response out so I'm going to uh take this and I'm going to do a little bit of formatting on these issues before passing it into the model so we can continue using more of these transforms that are built into AIP logic and if you've used pipeline Builder these transforms might look familiar where we have several of the transforms that exist in uh in pipeline Builder available in logic as well so uh in this case I'm going to use a transform to format my objects and I'm going to take in those relevant issues and I don't need that object Rd and I'm going to format it in an XML like structure and this is because it might be a little bit easier to have that markdown embedded in an XML like structure cuz we'll have these starting and ending tags that show where the issue starts and ends so in this case maybe I'll format it something like issue and slash issue and I can reference uh properties on this object set as I go so I can say all right great I want to reference this as uh the body in the middle of these tags and then I want to specify a couple more t uh details just as properties on this tag so maybe something like uh the URL and I'll put those in quotes So maybe I'll use the URL here and maybe uh the state uh so let's pass that in great and maybe finally the title so pass in a couple of things that will uh help us format this in a way that's a little bit easier for the model to uh to process so let's see what we get with these so I'm going to run just these blocks and we'll see that what we get out is this formatted structure that has these issue tags that have opens and then corresponding closes so it's a little bit easier for the model to identify okay all of this stuff in the middle everything from here till here is actually inside of this issue tag so this is a body of this uh issue that has this URL this state this title this is especially relevant as the complexity of these uh the inputs you're passing your model uh goes up so now instead of passing these relevant issues what I'm going to do is I'm going to pass an in those formatted relevant issues and I'm going to go ahead and run this again and so now we can see after uh after going through this run what kind of output we get cool all right so I'm going to uh actually talk L on the uh diff view that we have here to quickly see the changes and I can see even though the content is similar it's done a better job of formatting the uh the details here it's has a little bit uh more of an understanding of what the title is uh what the description is and so if once I go and look at this in markdown format it's a little bit easier to uh to read and what I can do is I can do some more prompt engineering here maybe I'll ask for uh it to use links or other things like that um but this is a good Baseline because we've provided the model with the context in a way where it's understandable and uh easy for it to interpret and grouped in a in a neat way so that it doesn't conflict with our instructions uh and the model is able to operate on it great so from here uh I can also go ahead and rerun our evaluations before merging this Branch we've made a couple of changes and I might want now go ahead and save it and merge it in um so I can run this evaluation Suite to see how has this changed between these runs so uh when I run it it will go ahead and take all of my test cases in this case I just have one test case it will go ahead and run those and we'll see some interesting things so if I flip through here I can say in our old run we were running at about 5.5 seconds and now we flipped to uh to running at about 2.3 seconds and I can click over to our metrics dashboard to see this a little bit easier instead of needing to flip back and forth I can actually pick a baseline run and a comparison run so I can go in and say all right our Baseline uh is that run we just did and our comparison was that last run our last run had an average duration of 5.5 seconds whereas now we're doing uh it in about 2.3 seconds so we've sped up by uh bit more than 100% And we can see a couple of other built-in metrics like compute cost and uh things like that see our individual test cases click into those test cases and see exactly what we're passing back and forth between the models see all those intermediate details that are uh that are going through see the comparison between them uh and then use that information to decide okay is this behaving the way that I expect should I be changing how uh this is uh what context I'm giving or uh the the tools that I'm using um or should I go in and Define a little bit more criteria for valuation here so instead of manually inspecting each of these test cases I can start defining automatic evaluations so in for this first test case uh maybe I want to give it a bit of a better name uh but I can also choose to add other parameters uh maybe I want to give it something like unexpected object to match and I can use several of these evaluation functions either some of the ones that are built in things like matching objects checking if strings are contained um things like that or uh some of our uh graders that we've bundled up for easy installation things like this rubric grader where I can specify a criteria of exactly what I'm looking for and then let uh the an llm uh evaluate using that criteria so this lets me uh build confidence that the changes that I've just made are actually behaving the way that we expect and so by doing that I can have confidence that once I merge this it will continue working in in production and so uh once I do that I can go ahead and start consuming it in all the places that functions can be consumed because this will publish just as any old uh other function so I can go and use this in uh a workshop application let's say I want to build an issue D duplication uh mini application I can put that together in a couple minutes um I can go ahead and use that in my own application so I can open the developer console and and call this from say uh my own internal ticket triage system maybe I have a uh handy command line helper that I use um or I can go and create an automation maybe I want to create an automation that every time a new GitHub issue is created I can automatically comment using uh this new logic that we've created so there's a lot of different ways of taking this once youve built it and applying it either within the platform or outside of the platform you can also keep extending this from here for example maybe you want to use the new conditional transform to be able to take different actions if an llm categorizes this as a bug report or a request for support so let's say if it's a bug report you do some of the D duplication that we've been doing and if it's a request for support uh you use documentation and you semantic search across that and then uh you give it some tools to be able to answer the user's question instead of finding potential duplic issues um so there's a lot of different ways we can take this and continue from here so that was just some of the capabilities in AIP to help you get to production uh expect more to come soon and try them out