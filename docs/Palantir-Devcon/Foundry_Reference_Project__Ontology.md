# Foundry Reference Project | Ontology

[Music] what is the ontology in foundry in this video we're going to try to pin down this sometimes slippery concept with a few examples from the foundry reference project now when we talk about a term like ontology it's easy to get wrapped up in big picture metaphors the ontology is a digital twin for your organization or it's a data api for structured access or it's a semantic layer for putting business meaning on top of your rows and columns now these framings all have their place and they're useful conversation starters when it comes to thinking about building value from foundry holistically however in the case of an individual project i often find it more useful to simply think about the ontology as the object layer where i've pulled data out of my data sets and structured it for fast access and user write back in my applications in this video we'll poke at the details of what differentiates data in the dataset layer from the ontology layer and see how structuring data in the ontology is the key to unlocking features that allow us to build applications that will compound rather than fragment our data asset let's go let's keep this practical lens as we take a look at the object types we've configured in our reference project continuing in our data lineage view where we left off in the last video on our right we see the final data sets in our pipeline and from them a one-to-one correspondence to an object type if we right-click on our example data route alert object type then we can jump into the ontology management application where this object type is configured before we get distracted by all the things in the overview let's reinforce what's happening technically at a fundamental level our object type configuration simply describes how to move data from our backing data set into a database and in that database each row in our input data set becomes an object in our destination object type and each column in our input data set becomes a property on that object type here we're looking at the property mapping every property on our object on the right corresponds to a column in our input data set on the left and each property has its own configuration to control its display stuff like how dates and numbers are formatted as well as conditional rules that can change the color and alignment based on simple logic now the overview should make a bit more sense we've got an object type called example data route alert and within that object type there are 1731 individual alert objects in addition to the properties that we just saw we've got two other key pieces of structure to add to this object type over here the actions are specific edit rules that define how users can edit these objects in this instance we've got a few state machine style edits that can change the status and assignee of one or more specific alerts as they go through their life cycle and below the link types relate this particular object type to other object types in our ontology here we see each alert is linked to the corresponding route each link type has its own configuration here we've got our alert object type on the left and our route object type on the right they're linked with a simple foreign key relationship the route id property on each alert needs to match the primary key property on the corresponding route if we instead had a mini to mini relationship we'd be prompted to choose a join table that defines the mapping between primary keys on both sides of the relation and we'll take a look at this configuration in a future video otherwise there's not much else going on in the relationship configuration the names config should have reasonable defaults but you can change them if you need to differentiate between link types that are making connections between the same two object types and here's a quick navigation tip to move around the connected parts of our ontology hover over the back home button and you can see related objects relations and actions let's jump to the route object type configuration just to see another example here we've got over 6000 routes and we can see more relationships defined both the relationship to the alert where we just came from but also relationships to the flight airport and airline object types enough poking under the hood what does all this structuring actually get us well one thing it gets us is a new way to search explore and visualize our data let's open up our routes in object explorer object explorer is sometimes overlooked by platform builders but it's got a surprisingly deep feature set for less technical users to explore and drill into data and often it's the perfect place to push users out from a structured application workflow into more open-ended exploration of the ontology data for instance let's say we want to compare all the routes that fly into two of the busiest airports in the u.s chicago and atlanta all of our charts are interactive so i can just click to filter down to the chicago routes i can also use my linked data following the relations that we defined in the ontology so for instance i can get just the routes that also have at least one alert that hasn't yet been resolved i can also save this investigation and here i'll just call it my chicago routes with alerts now that it's saved i can compare it with the exact same set except with atlanta routes that i saved earlier all of my charts now show the comparison between these two sets and i can see that atlanta's problematic routes are generally longer haul flights and the problematic routes in chicago are much more often cancelled so putting our data into object types unlocks object explorer for our end users and we can see that how we structure and build relations between our object types will dictate how they can navigate filter and investigate this data while object explorer lets users wrangle data at scale having our data in object types also unlocks the primitive of an object view let's check out some example object views and how they serve as containers to build rich reusable components for our end user workflows from our exploration we can use the results to jump into any individual route if you've created an object type before you might be familiar with the default object view that's automatically generated they look something like this and just lists out the properties and links for in this case the denver to atlanta route but as developers we've got the tools to make these views much richer for instance the carriers tab breaks down the flights on the route by the different carriers and the flight performance tab gives users an on-rails workflow to segment and compare different cohorts of flights that have flown along that route we can also traverse out the links for this specific route and for example hop into the object view of a related route alert here our object view is geared towards more operational use rather than the dashboards that we just saw for the route our alert is summarized and placed in context and we have access to some of the actions we've defined in our ontology to move this alert through its lifecycle in the next video we'll dive into how to build and configure these views as well as standalone applications you can find more detailed guidance on configuring your first object types in the foundry academy 301 course as well as in the ontology documentation and you can obviously continue to explore the other object types and their configurations within the foundry reference project on your foundry stack getting a useful grasp of the concept of ontology and foundry can be tricky it's easy to get sucked into abstraction and debates about how to organize data systems while this makes it tempting to throw up your hands and simply treat the ontology as a dumping ground for data of all shapes and sizes taking a little bit more time to think through your data model will pay dividends when it comes time to build your analytical and operational workflows as well as make every future project that much easier to implement subscribe to the channel to catch an upcoming video where i'll do a code review on my own ontology design within the foundry reference project and talk about what i'd do different if i could start it all over and also the next video in this series where we'll look at the application development examples in the reference project and see how the ontology structure we looked at today affects how we can build applications for our end users see you there [Music] you