# Building with Palantir AIP: Semantic Search

hi I'm Chad I'm an architect here at paler I spend most of my time helping our customers solve some of their most difficult business problems using different paler tools and so I see a big flavor across different customers different scenarios and today I want to share a couple examples of what we see let's dive right into the content for today so AIP on your Enterprise is the theme so our artificial intelligence platform and what it looks like to build integrated with an Enterprise ecosystem you might have as well all right let's look at the topics so a virtual tables that allows us to connect um different cloud data warehouses like big query snowflake or if you got parquet files or Delta tables sitting in a blonde store somewhere we can connect those register them in into our ecosystem and I can start building it's pretty cool we'll get into a little bit that looks like set up embedding generation so we have some free form text in that data how do I create embeddings our pipeline Builder which is our low code no code uh offering for building pipelines uh and then the ontology and how we store vectors in ontology and what that powers for us um and then semantic search so how do I get away from keyword search to searching based on the semantic meaning of things and then AIP logic which is really really awesome it's our L llm based functions across our ecosystem and how that plus our ontology really creates some prettyy cool experience and then our AIP assist is an application where we're going to build kind of that chat interface on top of your data using all the tools here that we're looking at all right so un unlocking your instruct dat these are the things that we're going to go build out today and so I'm going to use this to keep us honest as we go through we'll check this off uh and it's will be a little easier for us to follow all right so like I said we call this a cooking show demo reason is is I want to show you the end product first show you the nice steak dinner that we're going to make um and then we'll go actually build this from the ground up all right so this is a Workshop app that's our low code no code offering for building applications interactive applications as you can see here um this is a chat experience and we'll we'll put a prompt in here this is using some notional data we'll look at in a second but it's a manufacturing company called Titan that's our notional data company um and we have data on trouble tickets from the manufacturing floor about the and the resolution from Engineers how do we leverage that so we can help um reduce downtime by resolving issues quicker using past knowledge so all that's sitting in free form text Fields today how do we take that embedded knowledge in your business and leverage it like this so you can see I I asked a question about the ear loops on my Mas are coming off what should I do this is pretty cool because now I'm using llm functions that we're going to go build to navigate the ontology that is based on data sitting in bigquery so lots of really cool stuff going on here and it summarized those passive resolutions into things I should go look at to resolve my issue quicker so this this is what we're going to build and this is what you'll see in the end let's dive in first and uh go back to uh let's see virtual tables all right so virtual tables this is a great diagram it shows hey normally we would have had to sync data in today now with virtual tables you can just register it and pull it into your platform and start using it even though still sitting so no data copy all right big query so I said before um big query is one of the offerings here's a big query table I've got all this unstructured data these sample ticks I tickets I created um with the problem and the resolutions and so let's go we see what it looks like to register that so in here in our data connection you can see we can set up all sorts of different sources I think we have over 200 plus different data connections you can even create your own um and there's an SD K to build them if you've got some inhouse solution so what's the big query all right continue we'll connect up here so we're going to set up a direct connect so this is going to allow us to go from our Foundry instance whatever hypers skill it lives in can any of them ours happens to sit in AWS so I'm going to actually connect from AWS to Google bigquery in this example let's hit continue and I got an awesome big GRE data like that's my existing environment and we are going to this here and our data connection folder we'll keep things nice and clean all right cool and here's where I put in my Google project ID and I've already set up an i account a service account over in um gcp and so the service kind of has access to the big query tables I showed you and then here um since we know you're trying to connect to bigquery here are the five ports that we need to make that happen and so we're very explicit to show you that here's the different egress policies that you need to allow Foundry to reach out um to to gcp right done all right I I created The Source um now let's start registering some virtual tables all right let's find a home for it they up as you can see I like to keep things clean for all of our architect fillows um all right project ID so right now it's it's getting the metadata from the catalog so this is the initial setup all right so we've got the metadata from the catalog and big query and here's our trouble ticket table I'm going to call this just trouble tickets and hit create awesome all right so I think that was like two minutes um so let's just go knock that off a list of things to build cool all right so this is uh a virtual table you can see here is the the data that's sitting in big query but it's actually now available here and so um you can see the source and everything were very explicit I'll actually show you here um the table is stored in big so I'm not incurring storage cost this just gets to live in big prate no d a duplication all right I hope I've hammered that home that point home uh let's go create a pipeline all right so again for my architect fellows let's keep this nice and clean to our pipeline here if I can spell all right oh so we've got bash pipeline we can also do streaming but for today we're going to do batch so this this is pipeline Builder this is our low code no code um environment to build out these skillable pipelines it's backed by a patchy spark and Flink so for streaming and batch for micro batch um you can do incremental builds all that kind of stuff um really cool because I can build these these super easy pipelines but they can scale to huge data set so if it's you know the 17 rows I have in the D data set here but I've also used virtual tables with pipeline Builder on tens or hundreds of billions of rows personally and I see this same type of experience but we'll go with this one today and so in in uh pipeline Builder here we're going to clean up some things and you can see I can transform join Union things I can also connect uh a snowflake table to a big query table to something that's already sitting inside of our paler tools so um you can do all those in one pipeline today we're just going to clean up this table from uh virtual table connection so these two little purple icons though these are actually backed by AIP assist so I've got to generate and explain so these are LM backed functions so we're actually going to generate Pipeline with natural language text so I've already put one together for you here and so I want to replace special characters anytime I've got uh this stuff coming in I want to make sure I don't have special characters and strings for embeddings and other reasons uh then I also personally I like typ case stuff so I'm going to I'm going to make my status column to be typ case all right so let's hit generate so what's actually happening is it's taking the metadata about my existing pipeline the metadata from the Source tables and the prompt and the intent it's use it's going through our language model service so it's got a secure connection and then we have our own private logal instance of gp4 um for this use case you can also bring your own models we can get into that in another video but for today we're going to use gp4 um it's going to go construct the pipeline here and we'll actually have a usable pipeline all right done so you can see our goal is actually to show transparently what's Happening you can always see the steps that an LM might be taking and then here we also see the the output and I can take that output and run with it modify it or just take it as it is and so these are different boards so these boards allow us to do different transforms and here it actually generated Rex so it generate Rex to replace special characters with empty strings just like I had asked it did that across all the string fields in my data set and then as I had asked I wanted to make the status column type case so very cool these boards you can start to stitch together another neat thing is you can see it actually did those trans formations and I can see a preview of the output of those Transformations so as I'm going I don't have to build the whole Pipeline and understand is this working is it doing what I intend I can actually see it bu going as I'm building a pipeline out all right let's go back to the graph um it says transform path uh if anyone knows me I'm terrible at documenting things properly um but having L by my side I can actually have it know the intent of what I'm trying to do cleans and formats ticket data it actually puts a description and title in here so the next person behind me isn't cursing me out let's go look at this here um so now we want to build a transform so um let's go look at that all right so we're going to do uh text embeddings so we talked about text embeddings um so this is an embedding board real quick before we dive into that I want to just talk about what is an embedding for anyone um embedding is when I take text I go through embedding model I get a vector and the output of that Vector is essentially the semantic meaning of the those words coming in and so you can see in this example here this is an open AI model that we use today um I have different animal things like the sounds they make um and they're all closer together so they're more related so the closer they are in the in the vector space the more related they are the farther apart the less related they are and so I've got a quarterback throwing a football totally unrelated necess to animals the idea here is now I can have the semantic search allows me to not just go on key words but the semantic meaning to find things that might be related but not using the same word this is super powerful when I'm trying to work across maybe different dialects I got people in South Africa and Boston and San Francisco using different terms for the same thing in my engineering group but I want to still learn across all of them so semantic search is a big key to that it's also how a lot of the search functions work on when you're shopping and they find products that are similar um based on you type in crazy things and it will understand the semantic meaning of what you're trying to get and find products similar same that that's what we use in pettings for all right let's go back to build so text embeddings you can see this is using open AI uh adaa embedding model um in this example and so we're going to go pick our ticket description and we're going to have put a new column oops Bing there and then we are going to go do another embedding text to embeddings on the resolution all right let that run and so now I have these two new columns I'm creating the the different embeddings on those text Fields um it's actually making the whole calls to the Ada model uh through our secure endpoint again and it's going to create the vectors for us so you can see here here are those vectors from those new cols so now I've got vectors I got cleaned up data I want to put that into our ontology and we'll see what that looks like all right look at it's already warning me that I I didn't keep my documentation clean thank you all right uh new object type so we're going to create an ontology object and we'll talk through what that means in a minute Titan trouble tickets and we'll make it a little bug here and problems okay and let's pick an ontology that we're going to do this in BT architecture and then um my ticket ID is the primary key I want the ticket description to be the title property and then you can see here on the embedding vectors that we put in I want to pick how what function we use to do the similarity to tell the distance on that graph like we showing the distance between two vectors uh so we're going to use cosine similarity to do that awesome all right we'll hit save so a couple things I'll talk through here you can see our pipeline um what we would normally go do is hit deploy and let's actually look at some of those options so like I said this is backed by spark and Flink um in this profile I'm just use a single machine super easy but I can scale this up to extra- large profiles and then we even have custom profiles that can do you know as large as you want hundreds of thousands of nodes um to me do massive pipelines I've used this with 10 tens of billions of rows and scaled it up to hundreds of nodes but you can really customize it to fine-tune the performance awesome so normally got to play that it takes a few minutes so I'm just going to jump over to the finished one um that I have here so let's go to our data pipelines clean so this is what I already did you can see looks exactly the same it's already deployed though um so let's actually go look at that object and talk about the ontology for just a second so what is the ontology and so the ontology yes I have this structured data like we just talked about that we just build for the pipeline there's things I can do here though that's that's pretty cool it's differentiated so the ontology you think of is made up of data logic and actions so I have action types so I can create different actions so it's not just hey I have here's a trouble ticket but like what do I want to do that can I create new ones can I create an investigation can I close them I actually can define those action types they can write back to the ontology but they also can write back to Source system so if I have a trouble ticket system I'll write back to or a work order system to send a person out I can actually have those actions defined on that object and then um the logic piece so you can actually create functions uh inside of here that allow you to like embed models uh if I wanted forecasting models all this kind of stuff you can start to embed so the ontology is really data logic action that allows me to map out my business and so the goal of the on ology is to create the digital twin of your business process it's a realtime knowledge graph of how you operate so we get away from it defined terms and we get into more hey how how does my business actually operate what does the ground truth look like and that's the goal out of the ontology so we can create links to other object types um and really the goal is to map out how your business really operates all right so get off my soap box that's our uh our ontology here and so now let's go back back and talk about our like the next things we need to build here so we already did that we created embeddings so now we have an ontology object that's based on our virtual table sitting in bigquery I've created those embeddings ontologies there so now how do I create the ontology based semantic search all right I'm going to jump back over here and we are going to look at our semantic search so like I said before we're talking about embeding semantic search is so I can see the the actual semantic meaning of words not the key word them so this is code repo this is another tool inside of Foundry this allows us to build different functions I can do ones that do transforms like Python and Java and SQL um and here we're doing um typescript typescripts for functions so I can basically I can build this function and register it as part of that ontology object and so now again here is the uh you know I had the data in there I'm now going to create some logic about how I want to operate so this is the logic component um and so in theod repo here you can see it's normal things I on import the ability to like function with objects I want to get import the trouble tickets um object we just created I want to have access to the uh open AI imp petting model and we'll show you why in a second um but pretty cool stuff so like here I can see that these are the objects this is the the object type the properties that I have access to now in this code repo the other thing here is I've already registered this model uh model import so I've imported this actual live endpoint so it's pretty neat I already have some paler approved ones so these are ones that we've put out here again it could be Bedrock it could be your own model uh it could be all sorts of stuff and custom deployments here's just the one I'm used today I've already imported this and so that's a live endpoint I'm actually going to use part of this typescript function all right into the functions real quick I created two functions here one for searching trouble tickets and one for searching the resolution so search the problem and search the resolution and all I'm really doing is I'm taking user input and the K value the K value is uh find the five nearest neighbors or the three nearest neighbors so that's that's what the k value is and I'm going to return an array of trouble tickets so that's the input that's what I'm going to return um and here's where I take the user text input I send it to the the Ada um model endpoint and I get an embedding back for what a user might have entered in so now I want to take what a user enters in from a search as you remember in our in our nice stake D demo um I now I want to compare that against what's in the ontology so that's a search so I'm going to do a search on trouble tickets and give me back an object set so this is where we say take the ticket description betting that's the column we just created in the pipeline and I'm going to basically do a search and find the nearest neighbors the nearest K neighbors k value um from the embedding from the text that I is coming in to the ones that are in the ontology that's a search it's going to order by the relevance so order by give me the ones that are the closest to the farthest for the how many ever you want so this is the same function across both different columns and I can use those B coverings and we'll hit run and so that's actually doing a semantic search so this is showing you that this running this type script function is going to go find uh face masks um because it's similar to face coverings the semantic manab cool so now I've got a face uh I've got a semantic search function so normally I would go um Commit This and take a version I've already done that we've deployed out to ontology as you can see I'm using it here so if we dive into what's up next we've got a semantic search function that's awesome so let's mark that off our list and let's go back over and now I want to build a IP logic so let's go find our AIP logic app all right create new and we are going to go here create a new function so a logic like I said is an LM driven function for your ontology so this is what it looks like we'll get into how we actually go use it so we're going to add a string input so the string input would be the string coming from the chat feature I showed you in the the end demo um so this is going to be the problem it's going to be a string we can add different blocks whether it's variables or Properties or we can use LM blocks that's going to be the fun stuff so you can see here in logic I can actually start to stitch together um logic blocks it's a great name all right so in here just real quick tour uh I can select different models so in here I can actually have one logic block for I'm using something one model versus another model and I can have various different models all working together based on maybe you have a model that's um you fine-tune your own llm or you've got your own model you want to use or there's an open source model that's really good at some function you want to use um you can actually use those prend you can see there's a whole list of uh here but you can also have your own or your own endpoint all right so got that now I want a system prompt of what do I want this thing to do um and so I've already written this here and so you can see you're an experienced engineer that can solve any mechanical issues this is important to take your time and get it right so um this is how I want the LM to behave and then we're going to give the LM tools well here are the different kind of tool options we'll walk them through through them real quick here so I have ontology functions that's actually what we just created we created uh semantic search ontology function we'll use here in a minute but let's go through the rest of the tools current date so it knows its place in time and space um I can apply actions so this LM like I talked about with ontology I have actions in the ontology now think about an LM could go create an action to go move a trouble ticket or push a work orderer out so actions allow you to actually move things forward and be really operational a calculator tool because we all know llms are terrible at math and then also query objects so um we give it access to very specific objects in the ontology so as you create the ontology or business uh hey I wanted to only be able to go use these five different ontology objects so you can actually specify and uh find or not find tune but limit down what the um scope of this lm's access is to your business uh today we're going to use the function we just created um and we're going to do the search trouble tickets that's the function we just created and so you can add multiple tools and here I'm just going to give it one tool for this example all right and then we're going to have what's the input that I want to go do so um here you can see I'm using variables this variable is actually the variable coming in from up here my problem variable so that's the the problem that an engineer is facing um and that's what's going to feed in here so I'm going to say search for similar ticket based on the question from the problem variable and look across these trouble tickets that are similar and find resolution for those and then summarize that for things that most likely will fix it and then use it on bullet point so I tell it how I want it to go what I want to go do and it has access to this tool to do a semantic search across the ontology cool and then so the output of this is the resolution it's going to be a string awesome so these things are great everyone knows ens are non-deterministic so like being able to test them is hard so you think about AP logic there's there's really like I would say the debugger is kind of one of the coolest things so let's actually go test this out and see the kind of Chain of Thought reasoning that's happening here so I have an issue with face mask Loops staying on we're going to run so this debugger is actually going to show us both what goes in and out of each step what tools it's using so you can see this was the system prompt we put in here was the task prompt it decided to use the semantic Search tool um and so here was that semantic search the user text we had that K value we talked about um it found it based on the output of the semantic search we wrote and so these are the ontology objects that are most like it and then it's going to look at those tickets and the resolutions of how the engineer resolve those and summarize those into a nice result here so this is pretty pretty cool I had an llm actually go navigate my antology using semantic search and actually summarize all past resolutions so I can fix things quicker very cool so this is a simple function we'll have another video that goes in you can really get elaborate with um the blocks of functions so I could add another LM um like I said and you can output objects actions all sorts of stuff all right so let's go back and you can see here we created our a logic function done all right what's up next so let's go back over here to the end result how do we build this so we built all the pieces up here what is this so if I click edit here we're going to edit module so this is Workshop I think I had called that up before Workshop is the low code no code offering so drag and drop widgets that I then can configure on top of my ontology and so you can see this is a widget with a pro with a set of configurations that I can do this is another widget I can configure all these things by dragging and dropping moving things around so pretty simple way to go do this this cool chat widget um which I think was really what we're calling our co-pilot Constructor um this is where I can put hey this is your system prompt of the role of this agent and the agent has tools and so it has access to Titan trouble ticket objects has the tools and so if we click on this real quick you can see this is actually the function we created the AIP logic function the LM back function um that it has access to so now I've got a chat agent that has access to an LM backed function that has access to a semantic search function that can move across my ontology that's that's pretty freaking cool I'm just going to say it all right so you can do a bunch of different stuff and customizations about how this looks you can you can name it like I named ours uh Frank I love naming it what whatever you want you can have a little fun with it um use different icons a lot of different customizations in here uh and then this widget over here is our logic so this is like the Chain of Thought reasoning so as we saw in that debugger where we saw the Chain of Thought reasoning our goal is to be very transparent about what these LMS are doing and so the Chain of Thought reasoning is a great way so users can see what tools how did it get to the answer that it's providing me so I can build trust I can understand it maybe I want to at the object um and so that Chain of Thought reasoning is super powerful to do that so that's that's as easy as it gets here I could save and publish and I'm going to go view it op this out and um here we go so when I click this this is now I'll just walk you through this when I click this button it's going to take this text it's going to send it to my AIP logic function that we just created right and so now it's doing that and you'll see here so you can see Frank Frank's using the the user function tools so the resolve me um a IP logic function the LM back function and you can see the Chain of Thought reasoning here on the right is it's actually showing all the steps the objects I can go click on these objects investigate them and it give me a resolution and boom I got this nice text here you can also dig in here and look at the different um tools and pieces um lots of neat stuff you can do here but think about that now all this stuff that's buried in your unstructured data that's really hard to um unwind using traditional like bi tools or things I create these interactive experiences all right let's jump back over to our deck I think we finished all them awesome all right well I hope you enjoyed that we're going to do some more videos that will dive into more complicated use cases but I really wanted to show you something from end to end use case that we see across our customers and give you a flavor of like hey if I already have my Enterprise sources set up like how quickly can I actually get Val value with AIP and different paler tools you saw how simple it was here today of course you can add some more complexities and other things and we'll we'll dive into some of those things about how do I how do I tune my embeddings how do I create these more uh how do I tune prompts um all those kind of things will dive into in some other videos thanks again bye