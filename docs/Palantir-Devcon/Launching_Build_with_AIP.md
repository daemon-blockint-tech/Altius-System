# Launching Build with AIP

hi AIP Builders I'm Paul and I've been an engineer at paler for 15 years I'm here to introduce build with AIP a new Showcase of reference examples tutorials and starter kits that help you as a developer build new AIP powered applications as quickly as possible these tutorials are built on two decades of paler experience and we're putting those AI building blocks in your hands for the first time let's take a look at build with AIP so you can see how to get started here we have the AIP homepage this will be where you land when you first log into the platform and it's the first screen you'll see when you enroll as a new tenant um at the top of the screen we can see this build with AIP call out that's enticing us to click in and learn how to actually build as a developer let's do that as the page loads we'll notice immediately that it's packed with dozens of guided tutorials and detailed Hands-On examples that will teach us how to build AI powered applications this collection of starter kits All Ships on day one um I think the best way to understand the depth of each example is to click in so let's go to semantic search the starter kit at the top click in and see what we get okay each example here steps us through everything we need from data integration to creating and training AI powered models to building a robust ontology to ultimately wiring the end results up to a user facing front end um that will allow us to orchestrate real-time operations with AIP for semantic search that means we're shipping with a working data transformation pipeline that will show us how to ingest and extract text from example PDFs it also means we're showing how to split those documents into meaningful chunks and then create semantic search embeddings with included AI models you can of course swap those out and bring your own uh ultimately this all culminates in a working document search UI that's included so we can best see how this all wires up to a user facing application in short everything I need to get started with AIP is included in these starter kits build with AIP is available now to any developer with an IP enrollment stay tuned in the coming weeks where we'll ship additional videos walking through these reference examples in depth we're excited to see what you build [Music]