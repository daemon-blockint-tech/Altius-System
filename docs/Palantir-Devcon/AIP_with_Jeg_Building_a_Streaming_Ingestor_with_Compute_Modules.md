# AIP with Jeg: Building a Streaming Ingestor with Compute Modules

hey my name is Jag I'm going to be running through building a data ingestor using compute modules um I've got some very exciting data I found some open bus data from the Department for transport in the UK uh we're going to look at how we're going to pull that data using typescript we're going to look at how we put that into a stream and then get that to persistently run inside a compute module in AIP the first thing I'm going to do is go through the bus open data service for bus timetables vehicle locations and fairs um the data I'm actually going to pull is the live location of buses and we're just going to see how far we can get with it what data we have available and then start visualizing it inside AIP sweet so the first thing I've done is I've made an empty typescript repository and I am going to create a source folder and add my index.ts so we're coding in typescript there are a few things that I'm going to need I'm going to need typescript uh at some point I'm going to use xml2js I also want um the types for that uh because this endpoint actually gives me XML I will want typescript node so I can run locally and noon we'll see later how I use that for testing uh but for now let's just install all of these things and hope this works looks good to me the other thing I want to do is use typescript so let's initialize a typescript config file this defines where my typescript is going to be uh output so here I can see that I have uh an out directory instead of just in the middle of nowhere let's put that in dist and we are going to make a folder called dist sweet the other thing I want to use is git so let's initialize git let's make a git ignore file so I'm using get to uh store my repository and there are certain things that I don't want to be uh stored uh the node modules which will be installed whenever we run build uh or run install and then the disc files or the distributable which will be created whenever we run build cool so let's start by creating our build function hopefully this just works never does uh but if we're lucky and I've written the right thing we should just get like us strict amazing so my typescript is being converted into JavaScript that's what we're going to run in node we're going to build some nice helper functionality here so we're going to add a Dev command and in our Dev command we're going to run node one wonderful hopefully this works too ah that sheer confidence um sweet so the first thing we're going to do is create a function called get bus data and that function is going to take an API key because this uh endpoint takes an API key so uh they've got this nice test harness thing uh on their website the the link is somewhere here here maybe I'll share a link somewhere on this video who knows um but if we click try it out we can see there are operators we don't want any of that we don't want a specific vehicle ref just going to bank all this out and hit execute and we have a request URL that we can use and an example of an output uh a 200 undocumented so I'm going to copy this thing and the default values shown are for the Liverpool area you can change this if you want me personally I I prefer seeing buses from Liverpool so let's do that so let's create a const request URL and we'll call it this guy um now this API actually requires an API key you may be looking at bounding boxs going like that's going to be in camel case you're wrong it's obviously going to be snake case and I've actually put my API key in my environment variables you may be wondering why I did that not in front of you it's because I don't want to leak my personal data cool so actually let's do this shove API key in there and let's run a fetch so fetch it wants a URL check that out I think that will work I think that might be the whole thing uh we'll we'll see how wrong I am here where I can call get bus data let's make this function async and then then process. m. DFT API key so let's put that API key in there type safety is unhappy so let's do that and then X console DOT log x.

Jason nope it's not going to be Jason I just told you it was XML so x. text and then x console.log x my variable names are beautiful now why are are you not working uh you think maybe I just need to do this Source index.ts started clean exit cons log here who said this was going to be smooth okay well obviously I just need to restart this process ah amazing cool so we have a load of dat here um no point in scrolling through it that console log isn't useful we can see that actually all of that is in text so let's do this X and then uh x. text so actually this is an async function so raw data equals a wait and then con text or XML data equals wa ra data.

text and now we're going to change this into Json so let's import pass promise there we go cool um cool and we're going to return P string promise of that XML data and the one thing I do remember is explicit array I want to set to false so XML itself doesn't really Define how many objects are under every grouping uh when you don't say explicit array then it will just say that everything is an array of everything it becomes quite frustrating um but we should be able to do this now oh look at that isn't it shocking when it just works uh so let's call this guy fch bus data and then we can call this const bus data Json Json data cool so we're going to want to return some things from here um I'm going to do some very lazy splunking so the one thing I want is the timestamp and you can see that that's under Siri do service you're not helping me Json data. siri. Service delivery response timestamp why are you unhappy with me H I need to shove another await in and then the second thing I want is this vehicle monitoring delivery thing uh I'm assuming that it has the vehicle activity in there so let's do it would have been sensible if I put any sort of AI assistant in my IDE but I'm going at [Music] blind cool sweet look at all those objects let's add the web vehicle activity and nice so we actually have now managed to pull out all of this data into Json the reason this is useful is when we stream data into Foundry uh it's accepted in a struct or a Json format so it's a lot easier to work with it as uh as Json the truth is I would be able to just use our pipeline Builder to do this but I wanted to do some pre-processing and you can't stop me um sweet so now we have something that obviously pulls the data we now want some uh process to put it into Foundry the way we can do this is if I click new and stream and then I call this guy DFT bus ingest stream uh we don't need to mention it's from Liverpool not that I have an issue with Liverpool just just for the demo um cool now I'm going to get this vehicle activity and I want adjacent sample of it so the first thing I'm going to do is get that example back in process.

m. DFT API key I told you this wasn't practiced right so we're doing it live and then X and console.log uh X do vehicle activity Z and then we want ad Jason stringify it so I don't see all of this like uh valid nonsense please tell me that's going to give me something useful cool now watch The Wonder of Foundry I don't even need to know my types I can just generate a type from sample now I actually don't want all these extensions these are specific to Regions um but what this will do is it will validate the schema when a row is put in everything is nullable so we should be safe oh I hit back can I hit forward yeah let's do it again so fast um I'm going to set the throughput to normal I don't think I'm putting more than 5 megabytes a second in I think this is everything that I care about I'm just going to hit create stream and then we wait cool so now I have the opportunity to connect via API we're writing in no. JS and even though there's a nice guide here on how to get a token I'm going to just blank out the token because we know what we're doing um so this is the sample code it's given me let's just copy that straight in uh and we will create a new async function put it somewhere useful async function push row to Foundry and let's do this so things that are important boundary token so let's shove token in here as an input we can put the word token in there uh we don't need a sample um we'll leave this URL in here for now and we should be able to then return fetch post URI token value so we're going to need some rows rows is an array of any and and you see how this example takes one row check us out we're going to do many so for each row we're going to map that guy to Value row I think that would work really I don't need to conso log this data maybe we'll catch that error who knows I don't actually need any of that look at that look how simple this has become right so we're going to fetch bu data and then let's make a little main function look at me it's like I'm writing Java all over again so we're going to con data equals fetch Plus data we're going to put that process it's literally right here I just I just love typing and then we are going to push row to Foundry I've already put put my token into my environment variables why is that Chris oh because I don't want to leak my token uh and then we're going to put the rows in here this is where things get interesting actually just I'm just going to put vehicle activity so data dot oh let's make this async I need to wait data so data adopt vehicle activity what are the chances that this just works I think I've said that before I don't have a lot of trust in my engineering ability um clean exit so if request ah aren we impressed look at that view this data set I now have data in Foundry uh it is this big struct we'll sort that out later but everything seems pretty good to me and let's make this run every 10 seconds set interval main let's call Main directly I'll explain why in a bit I have some forethought into my process um and let's put some logs in here so we know what's happening console.log fetching buses and then maybe we'll say how many buses we got console.log got data do vehicle activity.

length buses uh now if I only got one bus this log line would be wrong but we can put that into a GitHub issue I guess put in a wa here then rows pushed to Foundry cool so we have the simplest ingestor it's pulling data from uh the department for transport shoving it into Foundry running every 10 seconds I've written 38 lines of code so now for the hard part let's get it persistently running in Foundry using compute modules so first thing we want to do is create our friend the compute module so I went to New I hit compute module uh I'm going to hit next we're going to do pipelines mode do we want we we don't we do want an output yeah DFT ingestion stream let's call this guy output on Master hit add hit save what is next let's get started well we're going we don't need python so there's a nice guide in here um and we have this Library called compute modules um I'm going to daringly install the latest version so let's install that guy and we have an example of how to create a compute module so we can start with this bit of code and all we're doing is validating that this thing has been run in a pipelines mode if not then things are going to go terribly let's just end our execution now after that you can see we can get our build token and we've got an example of how to get service API so we're going to use all of these individual fun code pieces uh but the other thing I want is to add a Docker file so when we're making a compute module what we're doing is actually containerizing some code um in this example containerizing is describing the environment in which my code will run so I'm getting um a no. JS image I'm building for the amd64 platform that's really important if if it's not amd64 it won't work uh and this template will literally just run npm install there's a little bit of extra stuff we need to put in here which is mpm run build the reason we're doing that is we're using typescript so when you're using typescript we have this nice little compile stage and then this should mostly let me just correct this command mostly just work out the box let's see what changes we need to make so first things first the API key uh is not going to come from our process environment variables uh we are going to need a stream proxy URL so we won't be hitting uh the front door so to speak anymore so what will happen is we will do this uh maybe we'll save that front door so that we remember it for later just for the Memories cool and in instead we're going to do this and add stream proxy URL is that [Music] everything I think so cool so uh step one where are we going to get our credential so in Foundry we have credential stores we can describe what we call a source our source is going to be called DFT API and and the credential name is going to be called API key and you're going to tell me oh this could be null let's just ignore type checking you can tell me I am a bad person I don't care right compute module environment is we validated this up here ah let's do it then let's put token in here a string cuz this function doesn't know when it's going to be run and we can just put the word token in here cool so now we have the token it seems to be some other stuff oh and the stream proxy URL right stream proxy no let's do this compute module dot get service API Foundry service stream proxy I'm really hoping I'm getting this all right but okay so there are a few things to note here firstly inside the compute module we are going to say where services are uh these things are available to you there's other services uh this is very nicely typed um secondly we give you a token which is scoped down to the security of uh the things you put in as inputs and outputs so it can't access anything else but that's that's perfect like we like security and uh we can also get our secrets so we don't need to hardcode Secrets anymore not that you were doing that before cool so then when we call this function now it's type save we can put our build token in here and we can put our build token why do you do this to me H yeah where we can put our build token in here we just need to do this now some people may this I'm going like this code isn't very clean um I'm writing it Live come on come on right have I got everything I need I think so so let's create a repository my repository is going to be DFT containers hit create ambitiously implying that there are going to be more containers uh I can call this the DFT ingestor and we're going to build so this First Command takes that doc file gets all the files runs this plan uh if I've done everything right then this will just work and it won't throw you see that cool and then this command will then push that Docker file into Foundry and I think that's it so let's just quickly jump over um the command has appeared we can call this DFT ingester we can update our configuration and our compute module is almost done almost cool so when we go to the configuration page we can see there's an output we can see a container but we haven't put our secrets in so what we're going to do is create a generic source in Foundry let's call this the DFT Source uh we bundle our egress policies and our secrets together so when you're leaving the platform there's an intent that you're probably going to need secrets to do that hopefully if I hit confirm here oh no I'm assuming I already have a DFT API let good DFT Source was quite good confirm nice I did say I was doing it live right and there is already an egress policy I want to enable exports save this just means that we're happy with data leaving uh we want an API key in here now I'm very proud of this moment I already thought about the fact that I'd have to show my key on here in order to to paste it but what if I already put it into my command line that's good right no secrets leaked today today we are safe it is a safe day cool I hit save and we're aware that my source is wrong because I said DFD API so let's do this the Ft Source instead do a new release look how quick this is so 0.2 look at that we're doing so well we're already on version two if that isn't impact I don't know what is push these new layers and then bump this guy up to version two hit save Moment of Truth will it just run this is um anticlimactic ah maybe I'll make a pipeline who cares so while that thing's starting up what we're going to do is we are going to create a new pipeline so we've still got our stream kicking around I'm sure this is going to say running uh and the first thing we want to do is create a new pipeline so bft pipeline I'm very creative with my names now in pipeline Builder this is our in platform pipeline L Builder what great naming we have I'm going to take monitored vehicle journey I see it's a stru and then I should have something that's like flatten stru and then max depth too because I know that there are nested things in here I can hit accept I can hopefully see latitude and longitude nice so we've got the information I need in here I might even use AI to name that board while I'm here cool I'm just going to now create an object type so most things um let's call it DFT demo bu when you're interacting with Foundry you want to end up creating a yeah I I know that's not support by onology that can go cool um yeah when you're interacting with Foundry you want to create objects the onology is the base data layer in which we'll be interacting when we're making applications on top or when we're adding Compu modules so the first thing I want to do is create this concept uh there are very important things that are involved in this process including picking a good icon pick a train even though it's a bus because I live dangerously red busler um and then there was one other thing I need to do here right so we're going to create a Geo Point as well um how does one do this I create a go Point column I take the latitude and I take the longitude I hit apply I want an ontology GE point and I can take my GE point and I can hit apply and then in here I just want to see if I can add that GE point so add property GE Point generate properties what am I missing construct GE Point that's not what I wanted delete that delete that let's just call the GE point I think that's it so now we've created a bus I can hit save I can hit deploy and deploy so what we're doing now is we're reshaping that data that we've already landed in Foundry I very quickly just click through we're announcing that that data also includes latitude and longitude positions and we'll be able to now use this data on a map or in a workshop item uh just anywhere in the platform so let's go check out that object there's not that much more left to this demo because now we have this bus and it's streaming in there are a couple of mistakes that I made I I took this line ref instead of the vehicle ref as the primary key we can actually go in and quickly fix that so if I get the vehicle ref and I set as the primary key and as the title property I can hit save I can redeploy and the reason this is important is I'm assuming when I hit redeploy oh no it was actually fine with that the reason this is important is when we're putting data into the ontology we want to uniquely key all of our buses so I was actually keying like our buses on Route four rather than the individual number plates of buses um we all make mistakes I'm only human so I've made this new deployment and then the final thing I want to show you about these buses are if I go back into our friend the ontology I should be able to open an object explor and because we have this assumption that all of your data is GE points we've already been able to put it on a map for you using the object Explorer so now we can see all these buses just sitting around in um in Liverpool isn't that exciting cool so we've just gone through writing an ingestion ingesting it directly into a stream putting it into a compute mod module cleaning of the data in pipeline Builder and then finally what was the last thing I did oh yeah put it in the ontology