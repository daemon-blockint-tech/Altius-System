# Foundry Reference Project | Data Pipeline

There are so many ways that we could approach a&nbsp; new use case on top of Foundry. How do we even get&nbsp;&nbsp; started? When I'm working with developers I follow&nbsp; a bit of a homegrown solution design approach&nbsp;&nbsp; that folds together the functional requirements:&nbsp; that is, what's the new capability we need to&nbsp;&nbsp; deliver for our end users with the more common&nbsp; implementation patterns that we've seen time&nbsp;&nbsp; and time again work well across Foundry.

Taken together, these two considerations help&nbsp;&nbsp; sequence and architect our use case development&nbsp; holistically. Now, at some point in a future video,&nbsp;&nbsp; I'd love to subject you to all my ramblings about&nbsp; this approach. But for this introductory series&nbsp;&nbsp; I think we'll do something a little bit more&nbsp; straightforward and simply follow the data as&nbsp;&nbsp; it flows through our project.

In this video, we're&nbsp; going to introduce the code repository environment&nbsp;&nbsp; where we author the data transformation logic that&nbsp; forms the links in our pipeline. And then we'll&nbsp;&nbsp; see how the scheduler tool within data lineage&nbsp; lets us organize all of that emergent complexity&nbsp;&nbsp; into a pipeline that is reliable and performant&nbsp; for production use. Along the way we'll see code&nbsp;&nbsp; snippets and example pipelines that already exist&nbsp; on your Foundry instance as well as the training&nbsp;&nbsp; materials that are available to make sure you have&nbsp; a comprehensive introduction to all of these tools.&nbsp;&nbsp; Let's go.

Let's start back in the data lineage tool&nbsp; where we ended our last video. I'm starting with a&nbsp;&nbsp; slightly more fleshed out view of our pipeline.&nbsp; On your Foundry instance, you may already have a&nbsp;&nbsp; similar view saved as the aviation data lineage&nbsp; resource. So don't worry if you don't.

In the use&nbsp;&nbsp; case framing, the intent of our data pipeline is&nbsp; to prepare for the ontology layer. We'll get into&nbsp;&nbsp; exactly what this means in the next video, but&nbsp; for now let's oversimplify and just imagine raw&nbsp;&nbsp; source system data at the start of our pipeline,&nbsp; over here on the left, and semantic parsimonious&nbsp;&nbsp; data coming out the other end, here on the right. In&nbsp; between we're writing code in three rough phases:&nbsp;&nbsp; cleanup, enrichment, and structuring.

Let's zoom in&nbsp; and see if we can get a better understanding of&nbsp;&nbsp; what's happening inside one of these nodes. On the&nbsp; graph we see that this enriched flights data set&nbsp;&nbsp; has several inputs if we open up the code preview&nbsp; tab, we can see exactly where those inputs are&nbsp;&nbsp; defined in the transform df decorator in our&nbsp; transform definition. This means that I can access&nbsp;&nbsp; and manipulate those inputs as data frames inside&nbsp; my code.

To go further in the weeds let's leave&nbsp;&nbsp; our lineage view behind and jump into the code&nbsp; repository resource where this code actually lives,&nbsp;&nbsp; and step back a beat or two and define some terms&nbsp; we've used a bit loosely in the videos so far. At the top, we can see that we're now inside a&nbsp; resource called the flight control system repo&nbsp;&nbsp; that lives within our project folder structure.&nbsp; The repository itself has its own file system.&nbsp;&nbsp; On the left, in the files panel, we see two root&nbsp; directories: one called transforms_python, and&nbsp;&nbsp; another called transforms_sql. We already&nbsp; got the relevant flights.py file open,&nbsp;&nbsp; but alongside we can see other transforms&nbsp; defined in this same repository.&nbsp;&nbsp; The code contribution experience of the code&nbsp; repository ide should be familiar to most&nbsp;&nbsp; engineers who worked with git and managed software&nbsp; projects in the past.

You make a branch, iterate and&nbsp;&nbsp; test your changes, and make a pull request. And if&nbsp; these are new ideas to you, then you'll find that&nbsp;&nbsp; the specifics are well covered in our in-platform&nbsp; tutorials, webinars, and documentation, and that the&nbsp;&nbsp; core concepts are taken from tried and true&nbsp; software development best practices. Looking at&nbsp;&nbsp; our specific example code, we've got our inputs and&nbsp; outputs defined, and then, a compute function, where&nbsp;&nbsp; we've written a bunch of pi spark to join together&nbsp; our inputs and produce a final output data frame.&nbsp;&nbsp; Francisco will do a thorough code review of this&nbsp; in a later video, so let's avert our eyes for a&nbsp;&nbsp; moment and instead follow the output link to jump&nbsp; from the code into the data set resource itself.&nbsp;&nbsp; So now we have the concepts to understand&nbsp; our pipeline one level deeper each data set&nbsp;&nbsp; wraps together a set of files in the&nbsp; underlying distributed file system.&nbsp;&nbsp; Most commonly, these represent a data frame and its&nbsp; associated schema.

The job spec defines everything&nbsp;&nbsp; we need to create this data set. You really need&nbsp; to look at the job spec itself,  but if you do, you'll see the references to input data&nbsp; sets by their unique resource identifier,&nbsp;&nbsp; and the computation parameters that point to a&nbsp; specific file within our transformation logic.&nbsp;&nbsp; This is everything we need to give to the build&nbsp; system so that it can actually grab the relevant&nbsp; data execute the code and produce our output&nbsp; data set. And then, we have the code itself, which&nbsp;&nbsp; determines what actually happens inside our job&nbsp; which we call a transform.

And that's what we just&nbsp;&nbsp; saw defined within our code repository. And if&nbsp; we were to make a visual representation of that&nbsp;&nbsp; whole arrangement, you'd have the lineage&nbsp; view that we started this whole thing off with.&nbsp;&nbsp; Now that we understand how each individual data&nbsp; set derives from its transformation logic let's&nbsp;&nbsp; see how we can manage the emergent complexity of&nbsp; our entire pipeline. We can dip our toes into this&nbsp;&nbsp; by looking at the idea of a schedule.

For the&nbsp; Reference Project, we have one master schedule&nbsp;&nbsp; configured and when we open it up our coloring&nbsp; changes to reflect the schedule configuration.&nbsp;&nbsp; This is a pretty typical push mode schedule. I've&nbsp; got a set of target data sets to build at the end&nbsp;&nbsp; of my pipeline, and a set of trigger data sets to&nbsp; monitor for new data. In this case, my schedule will&nbsp;&nbsp; run whenever all of these trigger data sets&nbsp; have received new data.

And when that schedule&nbsp;&nbsp; is triggered — and I can also just do it manually,&nbsp; which is handy for testing and iteration during&nbsp;&nbsp; development — I leave it up to the Foundry build&nbsp; system to figure out the intervening data sets&nbsp;&nbsp; that need to update along with the right order to&nbsp; update them in. Each time the schedule runs, the&nbsp;&nbsp; details about performance and the version of code&nbsp; and data used are recorded in the schedule metrics.&nbsp;&nbsp; Now, the pipelines and best practices&nbsp; section in the code repositories&nbsp;&nbsp; concepts documentation goes into much more detail&nbsp; on how you should structure your pipeline and how&nbsp;&nbsp; best to configure your schedules for production&nbsp; execution. Make sure to check those resources out&nbsp;&nbsp; as you dive into your next project.

While this&nbsp; pipeline is just the starting point of our larger&nbsp;&nbsp; use case project, data transformation alone can&nbsp; be a topic of potentially bottomless complexity.&nbsp;&nbsp; As data scale, performance requirements, or logical&nbsp; complexity grow, you'll benefit from understanding&nbsp;&nbsp; more advanced topics in data engineering. Stay&nbsp; tuned, and subscribe to this channel to explore our&nbsp;&nbsp; other videos in this area: like how to encapsulate&nbsp; your pipeline logic into reusable libraries;&nbsp;&nbsp; or how branching in Foundry can isolate not&nbsp; just your code, but also your data; or how you&nbsp;&nbsp; can build incremental pipelines that save time&nbsp; and resources by only processing what's new.&nbsp;&nbsp; If you stick around you'll also see my colleague,&nbsp; Francisco, give my less than stellar PySpark in the reference project a thorough code&nbsp; review. You'll also get the next video&nbsp;&nbsp; in this series, where we'll look at the Foundry&nbsp; concept of ontology, and see how it serves as&nbsp;&nbsp; a structural layer between our data pipeline&nbsp; and our application development.

See you there.