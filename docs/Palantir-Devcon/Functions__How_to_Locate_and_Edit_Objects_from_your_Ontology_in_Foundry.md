# Functions | How to Locate and Edit Objects from your Ontology in Foundry

[Music] have you ever wanted to perform complex edits to your ontology maybe to edit a value across multiple objects edit the links in between objects or maybe create an entirely new set of objects in this video i'm going to show you how you can locate and edit the objects in your ontology throughout this video i'm going to be referencing objects and concepts from the foundry reference project so if you're not yet familiar with this i recommend you pause and go check out the overview linked in the description below and with that let's get started let's get started on a new branch for this change and call it feature air travel example functions then we can begin with the aircraft search example function we can see commented out here after uncommenting this function we can see codesys doesn't seem to know what the example data aircraft object type is so let's make sure we've imported it it still doesn't recognize the import and that's because the first step to interacting with your ontology through functions is to import object types and their links into your repository the ontology available to you will depend on the object types available in your organization in this case we want the aircraft object type but when you're working on the same foundry instance as others you might have permissions to see multiple object types with the same display name luckily we can check the ontology manager to confirm the unique api name for our object type we also want to select the object types representing flights airports and airlines so i'm going to go ahead and do that now finally we can click save to import the ontology types into the project now we can see codeassist recognizes our aircraft object type and we can even see the same description that we saw in the ontology management application since our code now compiles we can access our new function in the live preview feature but it doesn't really do anything yet so let's implement this our new function aircraft search example can make use of the objects.search functions api this api allows you to initiate a search for any of the object types imported into your project behind the scenes importing our example data aircraft type updates the interface with the information of our imported object this allows us to see autocomplete for all the properties and link types available to us here our goal is to get the set of all aircraft but since there are a lot of aircraft objects it'll be easier to display if we select just the first 10. to do so we can initiate a search for the aircraft object type order it by its primary key which in this case is the tail number and then take the first 10. now when we run our function in live preview we can see a set of 10 objects returned and hovering over one item will let us see its object view well what if we wanted to search for a specific aircraft the aircraft object type has a primary key based on its tail number property so let's implement a function that takes as an input and tail number first we can make a query for the example data aircraft object set we can then filter this object set by the tail number property and look for an exact match on the input tail number now rather than executing the query using the take or all method which would pull all the objects into memory in this case we want to return just the object set so we've essentially constructed a query but haven't yet executed it let's fix our return type to exclude undefined values and then let's go ahead and test our function on an example tail number live preview will actually execute the object set query so that we can see the result a single aircraft the ontology also allows us to define links between objects and these can be traversed via linked object properties or object set search arounds in the ontology manager is where you can define links between objects as we can see our aircraft object type is linked to many object types including airports flights and airlines so let's say we want to find the set of airports that this aircraft has ever arrived at to do so we need to know the set of flights it has or will take and from there we could find the set of destination airports for each of those flights this is quite easy with functions to implement this first let's construct a query for the aircraft matching the input tail number luckily we've already written this method next we can add a search around onto our aircraft query first we'll find all the linked flights and then from there we can find all the linked destination airports it looks like we haven't yet imported the link between flight object and the destination airport object so let's do that now we can search for the example data flight object type and then see the destination airport as a link that hasn't been imported yet then we can save to import it now code assist will autocomplete the search around for that link type calling all will then execute the query and return an array of airport objects we can test our function using live preview and the same tail number from the previous function after a few seconds we can see the list of 48 airports that have been the destination for this aircraft when using the objects.search api you can traverse ontology links via searcharounds but those queries don't actually load objects into memory until you make a take or all call however given an object you can access its linked objects using the link api properties you defined in the ontology manager for example let's write a function to return the current airport of a given aircraft similarly we'll use our method to create an aircraft query and then next we'll get the first item returned from our query and call it aircraft the set should be of size 0 or 1 since we filtered by primary key which either exists or it doesn't in this case we know it's going to exist so it'll be one next let's find the current airport by traversing the link between our aircraft and its current airport it looks like we didn't import the link type so let's do that now and then confirm that we got the api name correct calling get will actually traverse the link and get the object and finally we can test out our function and live preview using our favorite tale number it looks like the aircraft's current airport is philadelphia international in addition to writing functions that can read values or traverse linked objects you can also edit the properties and links between objects these ontology edit functions are authored using two decorators ontology edit function and edits let's implement a function to update the current airport of an aircraft after creating our aircraft query we can select our aircraft from our aircraft we can then get the link and then set the object across from that link to the current airport passed in in the function parameter in functions preview we can select our aircraft by its tail number and then set the current airport to a new one let's select uh denver after a few seconds we see that the aircraft object has been modified to set its current airport from philadelphia to denver now we've finished all the functions for this section so let's commit our changes and make a pr into master there we can publish our new functions now that our pr is merged we can go to the master branch and as you can see there are no publish functions so let's go ahead and cut a tag with a new version that's time 3.0 and we can go to branches and then tags to check the status of our publish after the checks pass we should be able to see our new functions in the publish tab of live preview let's run our update current airport function something to note is that when you run an ontology edit function in the functions helper in authoring edits aren't applied to the actual objects the only way to update objects using a function is by configuring an action to use the function for more information on function-backed actions you should check out the documentation for function-backed actions in the description below let us know in the comments if you want us to create a video on that topic too congratulations you now know how to locate and edit objects from your urontology [Music]