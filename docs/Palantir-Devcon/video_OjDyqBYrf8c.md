# video_OjDyqBYrf8c

[Music] thank you many Enterprises may want users to use a corporate identity provider to lock into Foundry this enables Foundry users groups and more to be managed using already existing external systems whether you're just starting out or not if you want to configure or set up SSO in Foundry let me show you how here I'm in Foundry control panel to configure SSO first we want to go to authentication section under enrollment settings here we can see a list of currently configured SSO providers we cannot see which one's currently enabled and is also possible to manage and test each provider individually let me show you how to link to a new SSO provider in Foundry for this I'm assuming you're already familiar with the external provider you'd like setup so be only going through The Foundry specific steps to get started we just click add provider and then select a name now we are presented with the configuration page first let's manage the saml setup at the top here you can have the saml integration metadata you can copy these values manually but the it's easy just to download the file the first step is to upload or input these values into your SSO provider next you want to upload the identity provider metadata from your SSO provider here simply click the browse button and then select the file from your computer here we can configure which email device domains Foundry should use this provided for this is useful to provide users with a streamlined login experience as Foundry will automatically only show providers that are valid for a user's email the supported host option is an advanced setting if your Foundry enrollment only has a single domain the default settings should be fine at the bottom we have the attribute mapping here we can configure which user attributes in the SSO provider should be mapped to use attributes in Foundry ID and username are required attributes and in this case that keys are Name ID and Name ID respectively in the SSO provider recommended attributes included email first name and last name in our case we're going to set up email and our provider email is simply referred to also as email you can also add custom attributes and extremely usefully you can automatically import user groups from the identity provider this allows you to leverage already existing groups within Foundry enabling users to quickly onboard to whichever permissions they've already been granted to now the sample is configured we have two more options organization assignment which is useful for environments for multiple Foundry organizations however if you only have one organization the default will be fine and finally the option to enable a provider we just configured it can be hard to be certain your SSL has been configured correctly to be able to avoid login issues found your has ability to test saml config here is the provider I configured earlier this is both enabled and working to create a test I click the test sample button and then create a new test in the panel that appears on the right after naming the test I can generate a unique link I can send this link to a user who's having login issues or I can click the button myself as I'm about to now this will open a new login window just as pasting a link would do I can then log in and then we go back to the saml testing page and we can see the login succeeded we can see the steps it went through and most importantly we can see what attributes were sent and mapped by Foundry at the top here we can see attributes mapped into Foundry these will look good and usually you can see unmapped attributes this can be useful when the SSO provider you're using is documentation is not super clear finally if you get stuck there is excellent documentation on configuring SSO and Foundry in control panel simply click the help link in the bottom left left and it will take you to the public Docs here under authentication you can find an overview of how authentication Works how to get started and usually step-by-step guides with screenshots on configuring a zero ID and OCTA as SSO providers in Foundry I hope you found this helpful and you now find it easier to set up SSO in Foundry [Music] thank you