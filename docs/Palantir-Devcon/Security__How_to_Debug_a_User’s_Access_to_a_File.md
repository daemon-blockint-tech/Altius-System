# Security | How to Debug a User’s Access to a File

[Music] have you ever had to figure out why a user doesn't have access to a particular file and foundry due to foundry's unique permission inheritance both along the file hierarchy and data dependencies make it hard sometimes to figure out why a user doesn't have access to a particular file or the data within the file in this video i'll represent a data governance user and investigate two user permission issues where the users don't have access they need to do their work in my first example my colleague maria says she cannot see the data inside the flights data set and needs access to do a quick analysis to get started i will pull up the data set maria is looking to get access to open the security helper click on check access and input her name i can quickly see that maria only has the discover role on the project where this flights data set lives this means maria can see that the file exists but can't see the data inside the file clicking on the project and opening the security helper again i can see that there are three groups that are used to manage permissions to this project i will therefore add maria to the flight control system data source viewer group so that she has viewer access and can do her analysis let's move on to a second more complicated example in this example i have a different colleague named elijah who says he can see a flight alert inbox application but not any of the data inside of it like in our previous example i will first pull up the flight alert inbox application open the security helper select check access and put in elijah's name it looks like elijah has the viewer role on the flight alert inbox application which would be enough to see the application file but not necessarily the data inside of it so we need to dig a little deeper as the great information call it suggests let's explore the data lineage behind the flight alert inbox application [Music] after the data lineage opens up you might notice that the legend is automatically set to permissions when the data lineage is set to permission mode all the nodes in the graph will be colored based on the permissions of the view as user selected in this legend there are two permission types that are important to understand resource access and data access resource access is access to the file itself including the file name its column names and other metadata about the file while data access is simply access to the data within the file generally a user will need access to both resource access and data access in order to do their work in our example let's start by reviewing the data access permission type for elijah since the flight alert inbox application is colored white we know the data access permission is unknown for elijah so let's expand our graph to see the inputs of the flight alert inbox application to learn more once expanded we can see elijah's level of data access at each node in the pipeline the black nodes are objects in the ontology the red nodes are data sets back in the ontology objects as you can see elijah does not have access to the six input data sets all colored red in order to see the flight alert inbox application elijah would need data access to these six inputs to learn why elijah doesn't have data access we need to update the legend to look at resource access after the graph colors update we can see that elijah only has discoverable access to these six input datasets as you might remember from the previous example discoverable access does not give someone access to the data only access to the file name and its metadata so this is why elijah cannot see the data in the flight alert inbox application to fix elijah's access let's click on the discoverable text and scroll down the right panel to figure out the folder path for these data sets we can quickly see that all these inputs live in the aviation ontology project so let's open the aviation ontology project and then the security helper there we see that this project is managed by three groups to enable elijah to view the data in this project we need to give him at least viewer permissions to do this we will need to add elijah to the aviation ontology viewer group after doing that we can return to our data lineage and see that all the nodes are now blue this means elijah can view the input data to the ontology that the flight alert inbox consumes and he should be all set in conclusion it's important to understand the distinction between file access and data access there are many tools that can help you debug a user's access but typically the check access tool in the security helper and the permission legend in the data lineage tool provide the most complete picture of how a user is or is not getting the access they need to do their work [Music] you